/** Shared size scale (`xs`/`sm`/`md`/`lg`/`xl`) used by Button, Container, Section, etc. Components narrow this with `Exclude` to fit their CSS module. */
export type Size = 'xs' | 'sm' | 'md' | 'lg' | 'xl';
