'use client';

// Replace this stub with the real hook implementation.
//
// Drop `'use client'` above if this hook doesn't call any client-only React API
// (`useState`, `useEffect`, `useRef`, `useLayoutEffect`, `useSyncExternalStore`,
// `useContext`, etc.). `useMemo` / `useCallback` alone don't require it.
export function {{camelCase name}}(): void {
  // ...
}
