# Loaded by drizzle.config.ts (via dotenv) for drizzle-kit commands run
# from this package: `pnpm --filter database push|generate|seed`.
# Apps load their own env separately (see apps/web/.env.example).
ADMIN_DATABASE_URL=
