# Title — for <destination repo>

> **Purpose**: one writeup destined for ANOTHER repo — a PR to the template, an issue on
> a dependency, findings to upstream. Copy this file to `docs/upstream/NAME.md`.
> Naming the destination repo here is sanctioned — that is this folder's function; the
> leak gate skips `docs/upstream/`, and shipped content is re-checked by the
> destination's own rules. Everything else stays clean: no absolute paths, no OTHER
> private repos beyond the destination.
> **Audience**: the destination repo's maintainers/agents. **Committed**: yes.
> **Lifecycle**: when the PR/issue opens, mark this file shipped (add the link at the
> top) or delete it — an upstream doc that already landed is debt, not record.

- **Destination**: <repo>
- **Status**: draft | ready to ship | shipped (<link>)

## Content

<!-- Write it as the destination will read it: their conventions, their paths. -->
