# Decision log

> **Purpose**: dated one-liner decisions below the ADR bar — choices worth remembering
> that don't constrain future work enough to deserve a `NNNN-*.md` record. If a log
> entry later turns out to be load-bearing, promote it to a full record and link it.
> **Audience**: humans and agents. **Committed**: yes. **Written by**: agents, appending
> as decisions land; newest first.

<!-- Format, one line each:

- YYYY-MM-DD — decision, stated as fact. (who decided: owner | agent)
-->

- 2026-09-06 — Model routing profile set to `tiered`; safety-critical phases (allergy validation, auth, AI output validation) deviate to `quality-max`. (who decided: owner)
- 2026-09-06 — Product language is Spanish (`es-ES`); UI copy is Spanish, code identifiers and docs stay English. (who decided: owner)
- 2026-09-06 — Anthropic Claude via the Vercel AI SDK is the AI provider, chosen for first-class structured output against Zod schemas. Not yet wired — `ANTHROPIC_API_KEY` and `AI_MODEL` are reserved in `apps/api/.env.example`. (who decided: agent)
- 2026-09-06 — Env-var documentation mode: committed `.env.example` files, the template default. Nothing here is employer- or third-party-sensitive. (who decided: owner)
- 2026-09-06 — `apps/api` runs on port 3001; `apps/docs` moved from 3001 to 3002. (who decided: agent)
- 2026-09-06 — pnpm bumped 9 → 11.24.0 to match the installed toolchain; dependency build scripts are now allowlisted in `pnpm-workspace.yaml` under `allowBuilds`. (who decided: agent)
- 2026-09-06 — `configurations/eslint` declared its plugins as devDependencies, so `pnpm lint` failed everywhere with "eslint: command not found". Moved to `dependencies` and added the `eslint`/`prettier` binaries to each linting package. Reported upstream in `docs/upstream/`. (who decided: agent)
- 2026-09-07 — Fable is unavailable to the owner, so phases the `tiered` profile routes to fable run on opus at high effort instead. The routing table stays as written; the substitution is noted per plan. (who decided: owner)
- 2026-09-07 — Project 002 delivered: generation confirmed working end to end against a live database and a real AI provider. (who decided: owner)
- 2026-09-07 — A recorded health condition may cause an automatic dietary exclusion only where avoiding the substance *is* the definition of managing the condition. Coeliac disease excludes gluten; lactose intolerance is offered as a suggestion the user confirms; pregnancy and every other condition produce no dietary inference. Recorded as [`0008`](./0008-condition-exclusions.md). (who decided: owner)
- 2026-09-07 — Reuse is rotated per user and per plan version, and a user is never served last fortnight's dishes again; the prompt is told what they had and asked for a counted spread. Recorded as [`0009`](./0009-rotate-reuse-per-user.md), which amends [`0006`](./0006-reuse-before-generating.md): the cost claim bends for returning users until the library is large. (who decided: owner, by report; agent, the mechanism)
- 2026-09-08 — Recipes get an AI-drawn illustration, labelled as one, stored with the recipe and served from one public immutable route; off until the owner enables image billing. Recorded as [`0010`](./0010-illustrate-recipes-not-photograph-them.md). (who decided: owner, by request; agent, the mechanism)
- 2026-09-08 — Nutrition targets are advisory: a plan that drifts from them is delivered with the drift recorded, and only structural faults or safety bounds (minimum energy, protein ceiling) discard it. Recorded as [`0011`](./0011-nutrition-targets-are-advisory.md). (who decided: owner — "los datos son orientativos, nunca tiene que fallar la generación"; agent, the split)
- 2026-09-08 — When the model cannot fill a rotated pool, generation falls back once to the whole safe library — last fortnight's dishes included, no second model call — and records `fallback: full_library` on the plan. Excluding last fortnight ([`0009`](./0009-rotate-reuse-per-user.md)) is a variety preference and, like nutrition targets ([`0011`](./0011-nutrition-targets-are-advisory.md)), a preference never costs someone their plan. (who decided: owner — "nunca tiene que fallar la generación"; agent, the mechanism)
- 2026-09-08 — Ingredient alternatives are pairs between catalogue rows, seeded by hand in families, filtered per person by the same allergen gate as the dish, and shown on every meal — including plans already generated. A swap never introduces a class of food the dish did not have, which keeps a vegetarian's or a halal plan intact without the code knowing the pattern. Staples get none. Recorded as [`0012`](./0012-alternatives-are-catalogue-pairs.md). (who decided: owner, by request — "si una receta lleva lentejas, cámbialo por garbanzos"; agent, the mechanism and the pairs)
- 2026-09-08 — A third of every slot's pool is written fresh by the model whatever the library could fill, so two users' plans differ in content and the library grows with every plan; reuse fills the other two thirds. The catalogue grows from 200 to about a thousand rows, each with a food class, so the substitution rule and, later, dietary patterns can be enforced from data. Recorded as [`0013`](./0013-a-third-of-every-plan-is-fresh.md). (who decided: owner — "no quiero limitar nada, si no todos los planes se parecen muchísimo", and the fraction; agent, the mechanism)
- 2026-09-09 — A person can say once, on the meal screen, whether a dish should come back: liked dishes go to the front of the library pick and are named to the model as their taste; disliked ones are held back from reuse for good and named as never to recreate. One verdict per recipe, on the recipe. Recorded as [`0014`](./0014-a-verdict-shapes-the-next-plan.md). (who decided: owner, by request; agent, the mechanism)
- 2026-09-09 — A fortnight's plan may be redone once (the next fortnight is never rationed; redos are counted along the plan chain) and a plan's meals swapped five times, library first and the model only when the library has nothing for the slot; the shopping list is rebuilt in the same transaction. Two constants in `core/domain/Allowance`, for a premium tier to raise. Recorded as [`0015`](./0015-one-redo-a-fortnight-five-swaps-a-plan.md). (who decided: owner — the two allowances and their numbers; agent, the mechanism)
- 2026-09-09 — Generation asks each slot in its own request, in parallel, and covers a short first round from the library before asking the model again; the generation screen polls with backoff. A meal can be marked eaten or skipped. Recorded as [`0016`](./0016-one-round-per-slot-in-parallel.md). (who decided: owner, by report; agent, the mechanism)
- 2026-09-09 — Only an account with `email_verified` may use the product; the flag is set by hand while the provider is free-tier. A global guard after the session guard answers 409 `EMAIL_UNVERIFIED`, and the app sends the person to `/pendiente`. Recorded as [`0017`](./0017-access-opens-account-by-account.md). (who decided: owner)
- 2026-09-09 — The fortnight closes with a check-in, once per plan from its last day: the weight logs and the targets follow the latest weight; the portion answer moves the calorie target 5 % within the bounds; difficulty, satisfaction and their words go to the next plan's prompt. No answer is a restriction. Recorded as [`0018`](./0018-the-fortnight-closes-with-a-check-in.md). (who decided: owner, by request; agent, the mechanism)
- 2026-09-09 — The password-reset link is mailed over SMTP (nodemailer; four variables, all or nothing; Gmail with an app password until there is a domain) in the request's language, and the hook never throws so registered and unregistered addresses answer alike. Verification mail stays unsent on purpose: `email_verified` is the owner's switch (`0017`). Recorded as [`0019`](./0019-the-reset-link-travels-by-mail.md). (who decided: owner — real mail, Gmail as the sender; agent, the mechanism)
- 2026-09-09 — A progress screen reads what was logged and estimates nothing: the weight line on a time axis with start and target, and one card per fortnight lived with its meal marks (only days that have arrived) and its check-in. Adherence keeps the check-in's definition, eaten over marked. Recorded as [`0020`](./0020-progress-is-what-was-logged-shown.md). (who decided: owner — the next step; agent, the shape)
- 2026-09-09 — Earlier plans can be seen as they were and nothing about them can change: `/plan/historial` lists every plan lived, one opens in the same browser with marks shown and not offered, a meal of a closed plan has no controls, and the API refuses a mark on a plan that is no longer active as a conflict. Recorded as [`0021`](./0021-the-past-is-read-only.md). (who decided: owner — the next step; agent, the shape)
- 2026-09-09 — A swap may be asked for something: quicker, no cooking or more protein, each a filter on the library pick and on the model's answers alike, judged against the dish being replaced; cheaper and vegetarian wait for the data that could enforce them. Recorded as [`0022`](./0022-a-swap-can-be-asked-for-something.md). (who decided: owner — the next step; agent, the axes the data allows)
- 2026-09-09 — The end-to-end suites ran for the first time, against a throwaway database with a scripted model. They found that a free-text allergy excluded the matched row and not the food made of it: `madeOf` now excludes every catalogue row whose slug carries the anchor as whole tokens (`tomate` → `tomate-frito`, not `tomatillo`). Recorded as an amendment to [`0004`](./0004-deterministic-safety-layer.md). (who decided: agent, on evidence from the first run)
- 2026-09-09 — What a person will not eat is removed from the catalogue before the model sees it and from the library before reuse offers it: `ingredients.classes` records what a food is, `resolvePreferences` turns a way of eating and a dislike into excluded ids, and only what the catalogue could not resolve is still asked of the model. A user had written "pescado" in their dislikes and been served salmon. Recorded as [`0023`](./0023-a-preference-is-enforced-not-requested.md). (who decided: owner, by report; agent, the mechanism)
- 2026-09-09 — Six catalogue rows whose bare name left the form open are named for what a shopper buys: `Salmón fresco`, `Pavo fresco`, `Merluza fresca`, `Gambas frescas`, `Mantequilla con sal`, `Tomate fresco`. Slugs are unchanged, so nothing that matches on them moves. (who decided: owner, by report)
\n- 2026-09-09 — Failures reach the owner without waiting for a screenshot: 5xx responses and failed background jobs are reported to Sentry, off entirely without `SENTRY_DSN`, carrying the error, its stack and the route pattern and never a body, a user or a URL with ids in it. Recorded as [`0024`](./0024-a-failure-nobody-saw.md). (who decided: owner — the last item on the plan; agent, the mechanism)\n
- 2026-09-09 — An onboarding question must change something or it is not asked. An audit found five answers read by nobody: the day (bedtime, wake time, training days and time) now shapes the prompt, the cooking-time limit becomes a rule the pool and reuse both enforce, and the country is no longer asked. Recorded as [`0025`](./0025-every-answer-changes-something.md). (who decided: owner — "todos los datos del onboarding tienen que ser relevantes"; agent, the audit and the mechanism)
- 2026-09-09 — A swap can be asked for a vegetarian dish, on the food classes `0025` put in a column: no meat, fish or shellfish, eggs and dairy kept. The profile now says of each dislike whether it is kept out of the plan or only asked of the model, entry by entry, the way free-text allergies already do. Recorded as amendments to [`0022`](./0022-a-swap-can-be-asked-for-something.md) and [`0025`](./0025-every-answer-changes-something.md). (who decided: owner — the next two steps; agent, the mechanism)
- 2026-09-09 — What cannot be a rule becomes a weight: a dish from a kitchen they chose or using a food they said they like is offered by the library before the others, in the plan's rotation and in a swap alike, and nothing is excluded — so a narrow taste costs variety and never a plan. Recorded as [`0026`](./0026-a-preference-that-cannot-be-a-rule-is-a-weight.md). (who decided: owner — the next step; agent, the mechanism)
- 2026-09-09 — The green gate runs on a machine: `pnpm turbo lint ts:check test` on every push to `main` and every pull request. The deploy does not wait for it — the host builds whatever lands — so a red run is a record until a branch protection rule makes it a brake. (who decided: owner — the next step)
- 2026-09-09 — One mail a fortnight: a daily sweep tells accounts whose plan reached its last day that the check-in is waiting, writes the proof only after the provider accepted it, and carries no health data at all. The switch that stops it ships with it, on the profile. Recorded as [`0027`](./0027-one-mail-a-fortnight-and-a-switch.md). (who decided: owner — the next step; agent, the mechanism)
- 2026-09-09 — Every cron schedule is removed from `apps/api/vercel.json` while the project runs on free tiers: the two AI sweeps were already inert behind their own switches, and the check-in reminder — which spends nothing from the provider — went with them, because a scheduled call costs an invocation and one exception is harder to remember than a rule. The routes stay, guarded; §3b of the runbook says how to put one back. (who decided: owner)
- 2026-09-09 — The counters that stand between someone and unlimited password guesses move to the database: Better Auth's own limiter switches to `storage: 'database'` with a `rate_limit` table. The app's guard stays in memory on purpose — the paths that cost money are quota'd in Postgres already, and a write per request would buy nothing. Recorded as an amendment to [`0007`](./0007-own-the-rate-limiter.md).
- 2026-09-09 — Backups have a procedure and a script: `pnpm --filter database backup` writes one NDJSON per table plus a manifest naming the migration the data was shaped by, and §8 of the runbook says how the host's own point-in-time restore works and what neither covers. No restore script and no schedule, both said plainly rather than implied.
- 2026-09-09 — An admin screen at `/admin`, behind the stored role and 404 to everyone else, showing whether generation works and how big the catalogue is: counts, plans by state, and the last twenty-five generations with their failure codes. No user list, no plan viewer, no profile — those are how an admin surface becomes a way to read people's health data. Recorded as [`0028`](./0028-an-admin-screen-that-cannot-read-anyone.md). (who decided: owner — next on the roadmap; agent, the scope)
- 2026-09-09 — Every sign-up mails `OWNER_EMAIL` that an account is waiting, with the activation statement ready to paste; the hook swallows its own failures so a sign-up is never lost to a mailbox. It is the only mail that carries someone's address, because activation is by email. Recorded as [`0029`](./0029-the-owner-is-told-an-account-is-waiting.md). (who decided: owner, by request)
- 2026-09-09 — A confirmed address stops being a key: `activated_at` is the door and `email_verified` is the address, so the verification mail can finally be sent without handing out access. The owner's notice carries a signed one-click button, the admin screen shows the queue with one button each, and the error is renamed to say what it means. Recorded as [`0030`](./0030-a-confirmed-address-is-not-a-key.md). (who decided: owner, by request)
- 2026-09-09 — Two switches, not one: the door (`app_settings.registration_open`, thrown from `/admin`, refusing sign-up with a 403 the form explains) and the key (`activated_at`, an admin's act). Registration is open by default; a missing settings row must not shut a service. Recorded as [`0031`](./0031-the-door-and-the-key-are-two-switches.md). (who decided: owner, by request)
- 2026-09-09 — The web app's gates were still asking `emailVerified` after the split, so a confirmed-but-unopened account reached the app shell and `/pendiente` bounced it back to `/inicio`. `UserView` now carries `activated` and both gates read it. Found in production, not by a test. Recorded as an amendment to [`0030`](./0030-a-confirmed-address-is-not-a-key.md). (who decided: owner reported it)
- 2026-09-09 — The door now says who turns the key: with registration open, confirming the address writes `activated_at` and the person is in; closed, it confirms the address and an admin opens the account. Read at the click, never failing the verification. Recorded as an amendment to [`0031`](./0031-the-door-and-the-key-are-two-switches.md). (who decided: owner, by request)
- 2026-09-09 — Both locks, and one place to see them: the guard now refuses on either an unconfirmed address (409 `EMAIL_NOT_VERIFIED`) or an unopened account, `/pendiente` says which of the three waits it is, the owner's notice moved from sign-up to the moment an account actually lands in the queue, and `/admin` lists every account with the state of both. Recorded as a second amendment to [`0031`](./0031-the-door-and-the-key-are-two-switches.md). (who decided: owner, by request)
- 2026-09-09 — The door is retired. Sign-up is never refused; the one switch decides whether confirming an address opens the account (automatic) or leaves it in the owner's queue (manual), and it is renamed `automatic_activation` in code and database because it had stopped governing registration. Recorded as a third amendment to [`0031`](./0031-the-door-and-the-key-are-two-switches.md). (who decided: owner, choosing between two readings)
- 2026-09-09 — Vacation mode is a pause, not a second plan: declaring a trip moves every plan day at or after it forward by its length, so those dates hold no meals, nothing counts as skipped, and the check-in mail moves with the plan's last day without being taught about holidays. Cancelling gives back only the days not yet spent. Recorded as [`0032`](./0032-a-pause-moves-the-plan.md). (who decided: owner, choosing the pause over a generated holiday plan)
- 2026-09-10 — Analytics counts the rows and logs only what leaves none: an eight-stage funnel computed from state, so it is right retroactively and cannot disagree with the data, plus exactly two events for the things nothing records — a session started (sessions expire and vanish) and the axis a swap was asked for. No client-side writer, no content in an event, counts on the screen and never people. Recorded as [`0033`](./0033-count-the-rows-log-only-what-leaves-none.md). (who decided: owner — next on the roadmap; agent, the split)
