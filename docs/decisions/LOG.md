# Decision log

> **Purpose**: dated one-liner decisions below the ADR bar — choices worth remembering
> that don't constrain future work enough to deserve a `NNNN-*.md` record. If a log
> entry later turns out to be load-bearing, promote it to a full record and link it.
> **Audience**: humans and agents. **Committed**: yes. **Written by**: agents, appending
> as decisions land; newest first.

<!-- Format, one line each:

- YYYY-MM-DD — decision, stated as fact. (who decided: owner | agent)
-->

- 2026-09-06 — Model routing profile set to `tiered`; safety-critical phases (allergy validation, auth, AI output validation) deviate to `quality-max`. (who decided: owner)
- 2026-09-06 — Product language is Spanish (`es-ES`); UI copy is Spanish, code identifiers and docs stay English. (who decided: owner)
- 2026-09-06 — Anthropic Claude via the Vercel AI SDK is the AI provider, chosen for first-class structured output against Zod schemas. Not yet wired — `ANTHROPIC_API_KEY` and `AI_MODEL` are reserved in `apps/api/.env.example`. (who decided: agent)
- 2026-09-06 — Env-var documentation mode: committed `.env.example` files, the template default. Nothing here is employer- or third-party-sensitive. (who decided: owner)
- 2026-09-06 — `apps/api` runs on port 3001; `apps/docs` moved from 3001 to 3002. (who decided: agent)
- 2026-09-06 — pnpm bumped 9 → 11.24.0 to match the installed toolchain; dependency build scripts are now allowlisted in `pnpm-workspace.yaml` under `allowBuilds`. (who decided: agent)
- 2026-09-06 — `configurations/eslint` declared its plugins as devDependencies, so `pnpm lint` failed everywhere with "eslint: command not found". Moved to `dependencies` and added the `eslint`/`prettier` binaries to each linting package. Reported upstream in `docs/upstream/`. (who decided: agent)
- 2026-09-07 — Fable is unavailable to the owner, so phases the `tiered` profile routes to fable run on opus at high effort instead. The routing table stays as written; the substitution is noted per plan. (who decided: owner)
- 2026-09-07 — Project 002 delivered: generation confirmed working end to end against a live database and a real AI provider. (who decided: owner)
- 2026-09-07 — A recorded health condition may cause an automatic dietary exclusion only where avoiding the substance *is* the definition of managing the condition. Coeliac disease excludes gluten; lactose intolerance is offered as a suggestion the user confirms; pregnancy and every other condition produce no dietary inference. Recorded as [`0008`](./0008-condition-exclusions.md). (who decided: owner)
- 2026-09-07 — Reuse is rotated per user and per plan version, and a user is never served last fortnight's dishes again; the prompt is told what they had and asked for a counted spread. Recorded as [`0009`](./0009-rotate-reuse-per-user.md), which amends [`0006`](./0006-reuse-before-generating.md): the cost claim bends for returning users until the library is large. (who decided: owner, by report; agent, the mechanism)
- 2026-09-08 — Recipes get an AI-drawn illustration, labelled as one, stored with the recipe and served from one public immutable route; off until the owner enables image billing. Recorded as [`0010`](./0010-illustrate-recipes-not-photograph-them.md). (who decided: owner, by request; agent, the mechanism)
- 2026-09-08 — Nutrition targets are advisory: a plan that drifts from them is delivered with the drift recorded, and only structural faults or safety bounds (minimum energy, protein ceiling) discard it. Recorded as [`0011`](./0011-nutrition-targets-are-advisory.md). (who decided: owner — "los datos son orientativos, nunca tiene que fallar la generación"; agent, the split)
- 2026-09-08 — When the model cannot fill a rotated pool, generation falls back once to the whole safe library — last fortnight's dishes included, no second model call — and records `fallback: full_library` on the plan. Excluding last fortnight ([`0009`](./0009-rotate-reuse-per-user.md)) is a variety preference and, like nutrition targets ([`0011`](./0011-nutrition-targets-are-advisory.md)), a preference never costs someone their plan. (who decided: owner — "nunca tiene que fallar la generación"; agent, the mechanism)
- 2026-09-08 — Ingredient alternatives are pairs between catalogue rows, seeded by hand in families, filtered per person by the same allergen gate as the dish, and shown on every meal — including plans already generated. A swap never introduces a class of food the dish did not have, which keeps a vegetarian's or a halal plan intact without the code knowing the pattern. Staples get none. Recorded as [`0012`](./0012-alternatives-are-catalogue-pairs.md). (who decided: owner, by request — "si una receta lleva lentejas, cámbialo por garbanzos"; agent, the mechanism and the pairs)
- 2026-09-08 — A third of every slot's pool is written fresh by the model whatever the library could fill, so two users' plans differ in content and the library grows with every plan; reuse fills the other two thirds. The catalogue grows from 200 to about a thousand rows, each with a food class, so the substitution rule and, later, dietary patterns can be enforced from data. Recorded as [`0013`](./0013-a-third-of-every-plan-is-fresh.md). (who decided: owner — "no quiero limitar nada, si no todos los planes se parecen muchísimo", and the fraction; agent, the mechanism)
- 2026-09-09 — A person can say once, on the meal screen, whether a dish should come back: liked dishes go to the front of the library pick and are named to the model as their taste; disliked ones are held back from reuse for good and named as never to recreate. One verdict per recipe, on the recipe. Recorded as [`0014`](./0014-a-verdict-shapes-the-next-plan.md). (who decided: owner, by request; agent, the mechanism)
- 2026-09-09 — A fortnight's plan may be redone once (the next fortnight is never rationed; redos are counted along the plan chain) and a plan's meals swapped five times, library first and the model only when the library has nothing for the slot; the shopping list is rebuilt in the same transaction. Two constants in `core/domain/Allowance`, for a premium tier to raise. Recorded as [`0015`](./0015-one-redo-a-fortnight-five-swaps-a-plan.md). (who decided: owner — the two allowances and their numbers; agent, the mechanism)
- 2026-09-09 — Generation asks each slot in its own request, in parallel, and covers a short first round from the library before asking the model again; the generation screen polls with backoff. A meal can be marked eaten or skipped. Recorded as [`0016`](./0016-one-round-per-slot-in-parallel.md). (who decided: owner, by report; agent, the mechanism)
- 2026-09-09 — Only an account with `email_verified` may use the product; the flag is set by hand while the provider is free-tier. A global guard after the session guard answers 409 `EMAIL_UNVERIFIED`, and the app sends the person to `/pendiente`. Recorded as [`0017`](./0017-access-opens-account-by-account.md). (who decided: owner)
- 2026-09-09 — The fortnight closes with a check-in, once per plan from its last day: the weight logs and the targets follow the latest weight; the portion answer moves the calorie target 5 % within the bounds; difficulty, satisfaction and their words go to the next plan's prompt. No answer is a restriction. Recorded as [`0018`](./0018-the-fortnight-closes-with-a-check-in.md). (who decided: owner, by request; agent, the mechanism)
- 2026-09-09 — The password-reset link is mailed over SMTP (nodemailer; four variables, all or nothing; Gmail with an app password until there is a domain) in the request's language, and the hook never throws so registered and unregistered addresses answer alike. Verification mail stays unsent on purpose: `email_verified` is the owner's switch (`0017`). Recorded as [`0019`](./0019-the-reset-link-travels-by-mail.md). (who decided: owner — real mail, Gmail as the sender; agent, the mechanism)
- 2026-09-09 — A progress screen reads what was logged and estimates nothing: the weight line on a time axis with start and target, and one card per fortnight lived with its meal marks (only days that have arrived) and its check-in. Adherence keeps the check-in's definition, eaten over marked. Recorded as [`0020`](./0020-progress-is-what-was-logged-shown.md). (who decided: owner — the next step; agent, the shape)
- 2026-09-09 — Earlier plans can be seen as they were and nothing about them can change: `/plan/historial` lists every plan lived, one opens in the same browser with marks shown and not offered, a meal of a closed plan has no controls, and the API refuses a mark on a plan that is no longer active as a conflict. Recorded as [`0021`](./0021-the-past-is-read-only.md). (who decided: owner — the next step; agent, the shape)
- 2026-09-09 — A swap may be asked for something: quicker, no cooking or more protein, each a filter on the library pick and on the model's answers alike, judged against the dish being replaced; cheaper and vegetarian wait for the data that could enforce them. Recorded as [`0022`](./0022-a-swap-can-be-asked-for-something.md). (who decided: owner — the next step; agent, the axes the data allows)
- 2026-09-09 — The end-to-end suites ran for the first time, against a throwaway database with a scripted model. They found that a free-text allergy excluded the matched row and not the food made of it: `madeOf` now excludes every catalogue row whose slug carries the anchor as whole tokens (`tomate` → `tomate-frito`, not `tomatillo`). Recorded as an amendment to [`0004`](./0004-deterministic-safety-layer.md). (who decided: agent, on evidence from the first run)
- 2026-09-09 — What a person will not eat is removed from the catalogue before the model sees it and from the library before reuse offers it: `ingredients.classes` records what a food is, `resolvePreferences` turns a way of eating and a dislike into excluded ids, and only what the catalogue could not resolve is still asked of the model. A user had written "pescado" in their dislikes and been served salmon. Recorded as [`0023`](./0023-a-preference-is-enforced-not-requested.md). (who decided: owner, by report; agent, the mechanism)
- 2026-09-09 — Six catalogue rows whose bare name left the form open are named for what a shopper buys: `Salmón fresco`, `Pavo fresco`, `Merluza fresca`, `Gambas frescas`, `Mantequilla con sal`, `Tomate fresco`. Slugs are unchanged, so nothing that matches on them moves. (who decided: owner, by report)
\n- 2026-09-09 — Failures reach the owner without waiting for a screenshot: 5xx responses and failed background jobs are reported to Sentry, off entirely without `SENTRY_DSN`, carrying the error, its stack and the route pattern and never a body, a user or a URL with ids in it. Recorded as [`0024`](./0024-a-failure-nobody-saw.md). (who decided: owner — the last item on the plan; agent, the mechanism)\n
- 2026-09-09 — An onboarding question must change something or it is not asked. An audit found five answers read by nobody: the day (bedtime, wake time, training days and time) now shapes the prompt, the cooking-time limit becomes a rule the pool and reuse both enforce, and the country is no longer asked. Recorded as [`0025`](./0025-every-answer-changes-something.md). (who decided: owner — "todos los datos del onboarding tienen que ser relevantes"; agent, the audit and the mechanism)
- 2026-09-09 — A swap can be asked for a vegetarian dish, on the food classes `0025` put in a column: no meat, fish or shellfish, eggs and dairy kept. The profile now says of each dislike whether it is kept out of the plan or only asked of the model, entry by entry, the way free-text allergies already do. Recorded as amendments to [`0022`](./0022-a-swap-can-be-asked-for-something.md) and [`0025`](./0025-every-answer-changes-something.md). (who decided: owner — the next two steps; agent, the mechanism)
- 2026-09-09 — What cannot be a rule becomes a weight: a dish from a kitchen they chose or using a food they said they like is offered by the library before the others, in the plan's rotation and in a swap alike, and nothing is excluded — so a narrow taste costs variety and never a plan. Recorded as [`0026`](./0026-a-preference-that-cannot-be-a-rule-is-a-weight.md). (who decided: owner — the next step; agent, the mechanism)
- 2026-09-09 — The green gate runs on a machine: `pnpm turbo lint ts:check test` on every push to `main` and every pull request. The deploy does not wait for it — the host builds whatever lands — so a red run is a record until a branch protection rule makes it a brake. (who decided: owner — the next step)
- 2026-09-09 — One mail a fortnight: a daily sweep tells accounts whose plan reached its last day that the check-in is waiting, writes the proof only after the provider accepted it, and carries no health data at all. The switch that stops it ships with it, on the profile. Recorded as [`0027`](./0027-one-mail-a-fortnight-and-a-switch.md). (who decided: owner — the next step; agent, the mechanism)
- 2026-09-09 — Every cron schedule is removed from `apps/api/vercel.json` while the project runs on free tiers: the two AI sweeps were already inert behind their own switches, and the check-in reminder — which spends nothing from the provider — went with them, because a scheduled call costs an invocation and one exception is harder to remember than a rule. The routes stay, guarded; §3b of the runbook says how to put one back. (who decided: owner)
- 2026-09-09 — The counters that stand between someone and unlimited password guesses move to the database: Better Auth's own limiter switches to `storage: 'database'` with a `rate_limit` table. The app's guard stays in memory on purpose — the paths that cost money are quota'd in Postgres already, and a write per request would buy nothing. Recorded as an amendment to [`0007`](./0007-own-the-rate-limiter.md).
- 2026-09-09 — Backups have a procedure and a script: `pnpm --filter database backup` writes one NDJSON per table plus a manifest naming the migration the data was shaped by, and §8 of the runbook says how the host's own point-in-time restore works and what neither covers. No restore script and no schedule, both said plainly rather than implied.
- 2026-09-09 — An admin screen at `/admin`, behind the stored role and 404 to everyone else, showing whether generation works and how big the catalogue is: counts, plans by state, and the last twenty-five generations with their failure codes. No user list, no plan viewer, no profile — those are how an admin surface becomes a way to read people's health data. Recorded as [`0028`](./0028-an-admin-screen-that-cannot-read-anyone.md). (who decided: owner — next on the roadmap; agent, the scope)
- 2026-09-09 — Every sign-up mails `OWNER_EMAIL` that an account is waiting, with the activation statement ready to paste; the hook swallows its own failures so a sign-up is never lost to a mailbox. It is the only mail that carries someone's address, because activation is by email. Recorded as [`0029`](./0029-the-owner-is-told-an-account-is-waiting.md). (who decided: owner, by request)
- 2026-09-09 — A confirmed address stops being a key: `activated_at` is the door and `email_verified` is the address, so the verification mail can finally be sent without handing out access. The owner's notice carries a signed one-click button, the admin screen shows the queue with one button each, and the error is renamed to say what it means. Recorded as [`0030`](./0030-a-confirmed-address-is-not-a-key.md). (who decided: owner, by request)
- 2026-09-09 — Two switches, not one: the door (`app_settings.registration_open`, thrown from `/admin`, refusing sign-up with a 403 the form explains) and the key (`activated_at`, an admin's act). Registration is open by default; a missing settings row must not shut a service. Recorded as [`0031`](./0031-the-door-and-the-key-are-two-switches.md). (who decided: owner, by request)
- 2026-09-09 — The web app's gates were still asking `emailVerified` after the split, so a confirmed-but-unopened account reached the app shell and `/pendiente` bounced it back to `/inicio`. `UserView` now carries `activated` and both gates read it. Found in production, not by a test. Recorded as an amendment to [`0030`](./0030-a-confirmed-address-is-not-a-key.md). (who decided: owner reported it)
- 2026-09-09 — The door now says who turns the key: with registration open, confirming the address writes `activated_at` and the person is in; closed, it confirms the address and an admin opens the account. Read at the click, never failing the verification. Recorded as an amendment to [`0031`](./0031-the-door-and-the-key-are-two-switches.md). (who decided: owner, by request)
- 2026-09-09 — Both locks, and one place to see them: the guard now refuses on either an unconfirmed address (409 `EMAIL_NOT_VERIFIED`) or an unopened account, `/pendiente` says which of the three waits it is, the owner's notice moved from sign-up to the moment an account actually lands in the queue, and `/admin` lists every account with the state of both. Recorded as a second amendment to [`0031`](./0031-the-door-and-the-key-are-two-switches.md). (who decided: owner, by request)
- 2026-09-09 — The door is retired. Sign-up is never refused; the one switch decides whether confirming an address opens the account (automatic) or leaves it in the owner's queue (manual), and it is renamed `automatic_activation` in code and database because it had stopped governing registration. Recorded as a third amendment to [`0031`](./0031-the-door-and-the-key-are-two-switches.md). (who decided: owner, choosing between two readings)
- 2026-09-09 — Vacation mode is a pause, not a second plan: declaring a trip moves every plan day at or after it forward by its length, so those dates hold no meals, nothing counts as skipped, and the check-in mail moves with the plan's last day without being taught about holidays. Cancelling gives back only the days not yet spent. Recorded as [`0032`](./0032-a-pause-moves-the-plan.md). (who decided: owner, choosing the pause over a generated holiday plan)
- 2026-09-10 — Analytics counts the rows and logs only what leaves none: an eight-stage funnel computed from state, so it is right retroactively and cannot disagree with the data, plus exactly two events for the things nothing records — a session started (sessions expire and vanish) and the axis a swap was asked for. No client-side writer, no content in an event, counts on the screen and never people. Recorded as [`0033`](./0033-count-the-rows-log-only-what-leaves-none.md). (who decided: owner — next on the roadmap; agent, the split)
- 2026-09-10 — A plan is built from what you can buy: `ingredients.countries` (empty means everywhere; thirty rows of nine hundred name `ES`), the catalogue query drops what is sold only elsewhere, and onboarding finally asks the question now that something reads it. A country nobody stated filters nothing. English alongside Spanish was found already finished. Recorded as [`0034`](./0034-a-plan-is-built-from-what-you-can-buy.md). (who decided: owner — next on the roadmap; agent, the shape)
- 2026-09-10 — Two people's plans differ because the scheduler says so, not because the model wrote enough dishes to hide the overlap: `pickBest` breaks a tie by the pool's own order — that user's seeded shuffle — instead of alphabetically, and the library fills a slot's fourteen days instead of twelve. A plan the gate refuses is retried once with the whole library before the generation fails, which costs no call. Recorded as amendments to [`0013`](./0013-a-third-of-every-plan-is-fresh.md) and [`0009`](./0009-rotate-reuse-per-user.md). (who decided: owner, from two user reports)
- 2026-09-10 — `/admin` counts the provider requests that leave this service and measures them against a limit the operator configured, saying in its own copy that those are two different things — Google publishes no remaining-quota endpoint, and the binding constraint turned out to be twenty requests a day rather than tokens. The account list is paged and newest first: capped at two hundred oldest-first, it showed the founders and lost everybody who needed something. Recorded as [`0035`](./0035-the-panel-counts-our-own-calls.md). (who decided: owner, by request)
- 2026-09-10 — Two bugs from a phone. A paused plan is read-only and the controller is what says so — marking, swapping and verdicts answer 409 `PLAN_PAUSED` — because a disabled button is a suggestion. And the shopping list stopped being a snapshot: `shopping_list_items.name` was frozen the day the list was built, so switching language kept a Spanish list forever; names now resolve against the catalogue in the reader's language, with the stored name surviving as the fallback for anything hand-added. Recorded as an amendment to [`0032`](./0032-a-pause-moves-the-plan.md).
- 2026-09-10 — A profile says which meals somebody eats and how big each one is, replacing a count that could not name them: `slotsFor` took the first N of breakfast, lunch, dinner, so "two meals" always dropped dinner and somebody who skips breakfast had no way to say so. Four sizes a slot, weights underneath, and the scheduler unchanged because it already normalised over whatever weights it was handed. The backfill reproduces the old derivation exactly, so nobody is asked again. Recorded as [`0036`](./0036-which-meals-and-how-big.md). (who decided: owner, by request)
- 2026-09-10 — A box for what people think: a signed-in person writes to the owner, who gets an inbox with a reversible "handled" mark. The one admin read that carries somebody's words and address, because the message was written to be read and answered — nothing summarises it and it never reaches a model. Recorded as [`0037`](./0037-a-box-for-what-people-think.md). (who decided: owner, by request)
- 2026-09-10 — A tour of what is already here: five stops on the dashboard, shown once, each a title, a sentence and a link — the fortnight is planned, a dish can be changed along an axis, the meals of a day can be shaped, a trip pauses the plan, and the check-in sets the next one. The shopping list and the progress chart are left out because they are in the navigation bar. The mark is `profiles.tour_seen_at` rather than browser storage, so it does not reappear on a second device, and null means never — which is every account that existed before the tour did. Recorded as [`0038`](./0038-a-tour-of-what-is-already-here.md). (who decided: owner, by request; agent, the five stops)
- 2026-09-10 — Every API module gets the same five parts — module, `controllers/`, `services/`, `dto/in`, `dto/out` — and a route names what it takes and what it answers instead of returning whatever a literal happened to hold. The DTO question is settled without a second definition of any rule: a body DTO names the Zod schema in `packages/core/entities` that the web form already shares, and `@ZodBody` binds the pipe, the parameter type and the generated OpenAPI schema together. Responses stay status plus prose, because a hand-written response class is a copy of a core presenter that nothing checks. Recorded as [`0039`](./0039-a-route-names-what-it-takes-and-what-it-answers.md). (who decided: owner — the reference backend's shape; agent, the reconciliation with Zod)
- 2026-09-10 — One address per language: Spanish stays at the root, English moves to `/en`, and each language is its own root layout — so `<html lang>` is a literal, twelve public pages prerender instead of running on a function per request, and the English half of the product can be indexed at all, which it could not before. With it, everything a crawler was missing: `robots.txt`, `sitemap.xml`, canonicals and `hreflang`, a title per route, Open Graph with a generated image per language, and structured data built from the same dictionary the page renders. The cost, paid knowingly: first-visit `Accept-Language` detection is gone from public pages, because reading that header is exactly what made them dynamic. Recorded as [`0040`](./0040-one-address-per-language.md). (who decided: owner, choosing option B from three; agent, the three-root-layout mechanism)
- 2026-09-10 — What a page owes a reader who cannot see it: twenty-five WCAG 2.2 AA defects fixed, three of them decisions — a `--border-interactive` token at 3:1 because a control identified by a 1.26:1 edge is a control you hunt for; `--foreground-03` collapsed onto `--foreground-02` because on this ground **no grey is both legible and lighter than the secondary tier**, so the third tier survives as a name and dies as a colour; and topics under a section became headings at the size they already were. Also: the tour announced itself as an unnamed dialog and opened with focus on its own exit, and plan generation's live region did not exist in the branch that renders a failure. Recorded as [`0041`](./0041-what-a-page-owes-a-reader-who-cannot-see-it.md). (who decided: owner, by request; agent, the three trades)
- 2026-09-10 — What a paid account may spend, and one switch that decides the tier exists at all. The switches become a registry (`core/domain/Flag`) where every flag declares its row key, **which way it fails when no row exists** — `automaticActivation` on, because queueing everybody over a missing row fails shut for the wrong reason; `premium` off, because a missing row must never put a price in front of somebody — and who may read it, since an operational switch is nobody's business but the owner's. The tier itself is `user.tier`, granted by the owner, and the flag outranks the column: with `premium` off everybody is on the free allowances whatever their row says, so turning the tier off is one click rather than a migration. Premium raises redos from one a fortnight to three and swaps from five to twenty; nothing about safety, allergies or plan quality is behind it, and nothing ever will be. Payment capture is deliberately absent — it cannot be tested without a processor account, and an untestable payment path is the partial state this codebase refuses everywhere else. Recorded as [`0042`](./0042-what-a-paid-account-may-spend.md). (who decided: owner, by request; agent, the flag-over-column ordering)
- 2026-09-10 — A day that eats for something: an event — a race, a match, a Hyrox, a long session, named by the person — and the one to three days before it that eat for it. The person chooses the shape (which macros go up, down or stay) and never the size: `LOAD_STEP` is a fifth, one constant, on no screen, because the number is where the harm would live. A loaded day's targets are held to the same bounds as the profile's and a refused load is recorded rather than applied; there is deliberately no refusal keyed on a condition, per `0008`'s rule that this product does not write prescriptions. The plan day stores what it was built to and why, because the event may be deleted and the plan is history. Applied at the next generation — the scheduler gains a per-day override map and with none produces the same fortnight it always did. Recorded as [`0043`](./0043-a-day-that-eats-for-something.md). (who decided: owner, from a user's request; agent, size-in-code and the deferral of in-place rescheduling)
- 2026-09-10 — A fortnight rebuilt for an event: the in-place rescheduling `0043` deferred, premium only, plus a cap on events per fortnight (free three, premium ten; `429 QUOTA_EXCEEDED`, `kind: event`). Premium gets a second allowance, three mid-plan events a plan, and spending one rebuilds only the loaded days *strictly after today* of the active plan — from the library, never the model; through the allergy gate again; with the variety rules held against the days kept; the shopping list rebuilt; all in one transaction with the counter, which is a column on the plan (`mid_plan_loads`) because a count derived from `plan_days` cannot tell a load applied at generation from one applied mid-plan. Meals are updated in place so `meal_swaps` never cascades away. Every reason not to rebuild answers the same way — an empty `rebuiltDates`, the event waits for the next generation. Recorded as [`0044`](./0044-a-fortnight-rebuilt-for-an-event.md). (who decided: owner, the caps and the premium rebuild; agent, the counter's home, in-place meals and today-is-not-rebuilt)
- 2026-09-10 — Events move off the profile: an event is a fortnight's, not the person's, so it is declared where the fortnight is made — on the generation screen before a plan is built, and under a day of the plan for the paid tier's mid-plan addition. The generation screen stops starting on arrival, one more tap per regeneration, accepted. Recorded as an amendment to [`0043`](./0043-a-day-that-eats-for-something.md). (who decided: owner)
- 2026-09-11 — A day is built to all four of its numbers. The scheduler's fit function compared energy and protein only, so every day of a real plan missed carbohydrate by up to 46% and fat by 75% while hitting the other two to the gram, and no band existed to notice. Carbs and fat join the fit; portions are sized by exhaustive search instead of a greedy walk; a swap is judged by the day it becomes once re-sized; and validation holds all four macros to 5%, as guidance rather than rejection. Measured on the owner's own profile against the real library: fourteen days of fourteen inside 5% on all four, event days inside 1.3% of their raised targets, four seconds a fortnight. Recorded as [`0045`](./0045-a-day-is-built-to-all-four-of-its-numbers.md). (who decided: owner — "perfect, or it is no use"; agent, the mechanism and the advisory-not-blocking call)
- 2026-09-11 — A plan that misses its macros is scheduled again, from a wider rotation. `0045` measured fourteen of fourteen days inside 5% against the whole library, but generation hands the scheduler a dozen rotated dishes per slot (held short for `0013`'s fresh third), and the dishes carrying the carbohydrate were spent in the first week: days ten to fourteen of the owner's regenerated plan missed fat by up to 37%. When a plan misses a band, it is now scheduled once more from the pool it had plus the rest of the user's rotation, uncapped — same shuffled order, no last-fortnight dishes, no dislikes, the model's fresh dishes first, no second model call — and the plan that misses by less is kept. Reproduced on the exact rotation: a dozen per slot, fat 50% at worst; uncapped, fourteen of fourteen inside 5% on all four. Recorded as [`0046`](./0046-a-plan-that-misses-its-macros-is-scheduled-again.md). (who decided: owner — "analiza si está bien"; agent, the mechanism)
- 2026-09-11 — The full-library rescue no longer serves a disliked dish. It read the library with no rotation, so the path meant to spare somebody an empty fortnight could hand them a plate they had refused (`0014`). Asked whether the model could write a replacement instead: it mostly cannot — the rescue runs when the model has just failed, and the first pass already asked it for the shortfall. Dislikes are excluded, no extra call; last fortnight's dishes may still return; if nothing is left the person gets the honest error. Recorded as an amendment to [`0046`](./0046-a-plan-that-misses-its-macros-is-scheduled-again.md). (who decided: owner)
- 2026-09-11 — Dishes are designed to the whole split, per serving, at the person's own size. A second account's plan missed carbohydrate and fat on most days because its library had no dishes at its split: the prompt briefed every dish at the whole day's size (a share computed over the one slot in each request since `0016`), never mentioned carbohydrate or fat, and still claimed a plan short on protein is discarded. The prompt (3.0.0) now briefs each slot per serving on all four macros and fibre from the person's meal shape, states the split, teaches how a plate reaches it, adds plate guidance per goal and a share of dishes at any event day's split, and puts way of eating with allergy at the top. Recipes are stamped with a separate steps version (held at 2.8.0) so the change does not send the whole library through a model rewrite. Effect measurable only by regenerating. Recorded as [`0047`](./0047-dishes-are-designed-to-the-whole-split.md). (who decided: owner — "super profesional, nivel experto… para todo tipo de personas y objetivos"; agent, the brief and the version split)
- 2026-09-11 — Protein is held both ways, and the fortnight is repaired as a whole. A real 3.0.0 regeneration landed energy, carbohydrate and fat inside 5% every day and protein 7–17% over on nine, unreported: protein had only a floor and a safety ceiling. Validation now reports protein more than 5% over (advisory; the 3 g/kg ceiling still blocks) and counts it towards `0046`'s second schedule. The scheduler gains a pass after every day is built: days outside a band are re-sized to the bands, then trade a meal with the same meal of another day while that leaves fewer macros outside. The prompt (3.1.0) asks for protein as a figure to land on and for each meal's dishes to straddle it, as numbers. A penalty inside the day's own build, pool replacements in the pass and composition-picked rotation were measured and rejected. Measured on a production copy: the reconstructed pools reach 14/14 on all four; a real regeneration landed all four inside 5% on all fourteen days, events included. Recorded as [`0048`](./0048-protein-is-held-both-ways.md). (who decided: owner — "fixea también la proteína"; agent, the mechanism)
- 2026-09-11 — Generation reads the whole library, and a day's portion search has a ceiling. Loading a five-hundred-dish seed into development showed `findReusable` read 300 recipes with no order — a sample, not the library — and that a six-meal fortnight took 98 seconds to schedule in production code, against a 300-second function. The whole library is now read, in slug order; a day prices at most 59,049 portion combinations (what five meals already cost), six meals narrowing their lighter meals first; the spread pass prices at most a four-meal day's search. Six meals: 98 s → 29 s. Recorded as [`0049`](./0049-generation-reads-the-whole-library.md). (who decided: owner — the seed, dev first; agent, the read and the ceiling)
- 2026-09-12 — Generation can go through an OpenAI-compatible gateway. `AI_PROVIDER=omniroute` sends the pool builder's calls to OmniRoute at `AI_BASE_URL`, with `OMNIROUTE_API_KEY` and `OMNIROUTE_MODEL` (default the `NutrIA-Fallback` combo) beside each other in the environment. Structured outputs travel non-strict — strict mode made a model answer 400 to every request — a response that parses into the wrong shape is a provider error rather than a crash, and the SDK does not retry behind a gateway that already fails over. Measured on a demanding weekly benchmark: `muse-spark` and the combo keep 7/7 days within 5% on all four macros alongside the library, with no unsafe meal. Open before production uses it: nothing bounds a model call inside the 300-second function. Recorded as [`0050`](./0050-generation-can-go-through-a-gateway.md). (who decided: owner — "conéctalo a OmniRoute… el model se tiene que configurar por env"; agent, the wiring)
- 2026-09-12 — The pool prompt names an ingredient only where its slug does not already say it (3.2.0), and a slug returned with accents is read in the catalogue's spelling (3.2.1). The list was 70% of the prompt and 525 of 570 names repeated their slug with the accents back; input per call fell by a third (8.3k → 5.6k tokens) with every ingredient still offered. A model shown bare slugs wrote some back as "brócoli" or "calabacín", rejected as unknown; every catalogue slug is lowercase ASCII, so the dish schema folds accents before the lookup and the allergy gate runs on what it resolves to. Filtering the list by what recipes already use would save more (−39% for a person without restrictions) and was left out: an ingredient no recipe uses would never be offered again. (who decided: owner — "prueba la opción A", "arregla lo de las tildes… la versión será 3.2.1"; agent, the rule)
- 2026-09-12 — Every generation keeps a log of its model calls, and the admin reads it. Each call records the model asked for and the one that answered, the provider, our clock and the gateway's, the tokens with reasoning and cache, the cost, the gateway's request, correlation and combo ids, the session it was filed under — the job id, sent as `x-omniroute-session` to a gateway and to nobody else — what became of each dish, and for a failure its status and the quota Google writes into a refusal. It is written to the job row (`plan_generation_jobs.ai_calls`) as soon as the pool is built, so a generation that fails afterwards keeps it, and each call is one line in the server log. The admin gains the latest twenty generations with the address that asked — on a controller of its own, since it carries a person (`0028`) — and today's calls by the model that answered, with the last refusal and its limit. Not in it: OmniRoute's own limits and resets per model, which only its management API has, behind a key with the `manage` scope. (who decided: owner — "todos los datos que me pueda proporcionar omni… que usuario la ha realizado"; agent, the fields and where they live)
- 2026-09-12 — The model half of a generation has a time budget. `vercel.json` gives a generation 300 seconds in one function and nothing bounded a model call: a combo that failed hop after hop once took 645. Every round of calls now shares one budget — `AI_BUDGET_SECONDS`, 170 by default, which leaves room to schedule and save in the same invocation. The first round has all of it, a later one starts only with 15 seconds left, and a call still waiting at the end is cut and logged as a `timeout`; the library covers what the model did not bring, and the plan's metadata says the budget ran out. A host with no function limit raises it. This closes the open risk `0050` named. (who decided: owner — "haz 1 y 2"; agent, the figure and the rule)
- 2026-09-12 — A dish's time is held to the person's limit plus a fifth, rounded up to the next ten minutes: 30 admits 40, 55 admits 70, 25 admits 30. The minutes on a dish are an estimate — the model's or the recipe's — and the strict limit dropped dishes for running a minute or two over: time was a routine rejection across the week's benchmark, and a real generation lost its only model dinner to it. One function (`timeAllowance`, inside `withinTime`) decides it for the model's dishes, the library and swaps alike; the prompt still asks for the limit itself, so most dishes land inside it and the margin only saves the near misses. There is no lower bound — a quicker dish was always fine. (who decided: owner — "un 20% más, y redondea siempre para arriba… 34, pues 40; 66, pues 70")
- 2026-09-12 — A generated dish may carry fifteen ingredients, eight servings and six-hundred-character steps (prompt 3.2.2). A real regeneration lost its whole lunch: all seven dishes had thirteen to fifteen ingredients against a schema ceiling of twelve, while the prompt said "fifteen is a shopping trip". Validating every response the gateway kept that day: 77 of 628 dishes failed the schema — 41 on ingredients (none over fifteen), 11 on servings (all six), 10 on a step's text (404–561 characters), 17 on the method floor. The first three ceilings now match what `candidateDishSchema` stores; the prompt states the ingredient ceiling and the servings, seasonings included. The method floor stays: a dish that does not say how to make it is a quality rejection, not a limit. The step rewrite takes six-hundred-character steps too. (who decided: owner — "sí, y optimiza las raciones y los pasos"; agent, the ceilings)
