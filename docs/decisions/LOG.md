# Decision log

> **Purpose**: dated one-liner decisions below the ADR bar — choices worth remembering
> that don't constrain future work enough to deserve a `NNNN-*.md` record. If a log
> entry later turns out to be load-bearing, promote it to a full record and link it.
> **Audience**: humans and agents. **Committed**: yes. **Written by**: agents, appending
> as decisions land; newest first.

<!-- Format, one line each:

- YYYY-MM-DD — decision, stated as fact. (who decided: owner | agent)
-->

- 2026-09-06 — Model routing profile set to `tiered`; safety-critical phases (allergy validation, auth, AI output validation) deviate to `quality-max`. (who decided: owner)
- 2026-09-06 — Product language is Spanish (`es-ES`); UI copy is Spanish, code identifiers and docs stay English. (who decided: owner)
- 2026-09-06 — Anthropic Claude via the Vercel AI SDK is the AI provider, chosen for first-class structured output against Zod schemas. Not yet wired — `ANTHROPIC_API_KEY` and `AI_MODEL` are reserved in `apps/api/.env.example`. (who decided: agent)
- 2026-09-06 — Env-var documentation mode: committed `.env.example` files, the template default. Nothing here is employer- or third-party-sensitive. (who decided: owner)
- 2026-09-06 — `apps/api` runs on port 3001; `apps/docs` moved from 3001 to 3002. (who decided: agent)
- 2026-09-06 — pnpm bumped 9 → 11.24.0 to match the installed toolchain; dependency build scripts are now allowlisted in `pnpm-workspace.yaml` under `allowBuilds`. (who decided: agent)
- 2026-09-06 — `configurations/eslint` declared its plugins as devDependencies, so `pnpm lint` failed everywhere with "eslint: command not found". Moved to `dependencies` and added the `eslint`/`prettier` binaries to each linting package. Reported upstream in `docs/upstream/`. (who decided: agent)
- 2026-09-07 — Fable is unavailable to the owner, so phases the `tiered` profile routes to fable run on opus at high effort instead. The routing table stays as written; the substitution is noted per plan. (who decided: owner)
- 2026-09-07 — Project 002 delivered: generation confirmed working end to end against a live database and a real AI provider. (who decided: owner)
- 2026-09-07 — A recorded health condition may cause an automatic dietary exclusion only where avoiding the substance *is* the definition of managing the condition. Coeliac disease excludes gluten; lactose intolerance is offered as a suggestion the user confirms; pregnancy and every other condition produce no dietary inference. Recorded as [`0008`](./0008-condition-exclusions.md). (who decided: owner)
- 2026-09-07 — Reuse is rotated per user and per plan version, and a user is never served last fortnight's dishes again; the prompt is told what they had and asked for a counted spread. Recorded as [`0009`](./0009-rotate-reuse-per-user.md), which amends [`0006`](./0006-reuse-before-generating.md): the cost claim bends for returning users until the library is large. (who decided: owner, by report; agent, the mechanism)
- 2026-09-08 — Recipes get an AI-drawn illustration, labelled as one, stored with the recipe and served from one public immutable route; off until the owner enables image billing. Recorded as [`0010`](./0010-illustrate-recipes-not-photograph-them.md). (who decided: owner, by request; agent, the mechanism)
- 2026-09-08 — Nutrition targets are advisory: a plan that drifts from them is delivered with the drift recorded, and only structural faults or safety bounds (minimum energy, protein ceiling) discard it. Recorded as [`0011`](./0011-nutrition-targets-are-advisory.md). (who decided: owner — "los datos son orientativos, nunca tiene que fallar la generación"; agent, the split)
- 2026-09-08 — When the model cannot fill a rotated pool, generation falls back once to the whole safe library — last fortnight's dishes included, no second model call — and records `fallback: full_library` on the plan. Excluding last fortnight ([`0009`](./0009-rotate-reuse-per-user.md)) is a variety preference and, like nutrition targets ([`0011`](./0011-nutrition-targets-are-advisory.md)), a preference never costs someone their plan. (who decided: owner — "nunca tiene que fallar la generación"; agent, the mechanism)
- 2026-09-08 — Ingredient alternatives are pairs between catalogue rows, seeded by hand in families, filtered per person by the same allergen gate as the dish, and shown on every meal — including plans already generated. A swap never introduces a class of food the dish did not have, which keeps a vegetarian's or a halal plan intact without the code knowing the pattern. Staples get none. Recorded as [`0012`](./0012-alternatives-are-catalogue-pairs.md). (who decided: owner, by request — "si una receta lleva lentejas, cámbialo por garbanzos"; agent, the mechanism and the pairs)
- 2026-09-08 — A third of every slot's pool is written fresh by the model whatever the library could fill, so two users' plans differ in content and the library grows with every plan; reuse fills the other two thirds. The catalogue grows from 200 to about a thousand rows, each with a food class, so the substitution rule and, later, dietary patterns can be enforced from data. Recorded as [`0013`](./0013-a-third-of-every-plan-is-fresh.md). (who decided: owner — "no quiero limitar nada, si no todos los planes se parecen muchísimo", and the fraction; agent, the mechanism)
- 2026-09-09 — A person can say once, on the meal screen, whether a dish should come back: liked dishes go to the front of the library pick and are named to the model as their taste; disliked ones are held back from reuse for good and named as never to recreate. One verdict per recipe, on the recipe. Recorded as [`0014`](./0014-a-verdict-shapes-the-next-plan.md). (who decided: owner, by request; agent, the mechanism)
- 2026-09-09 — A fortnight's plan may be redone once (the next fortnight is never rationed; redos are counted along the plan chain) and a plan's meals swapped five times, library first and the model only when the library has nothing for the slot; the shopping list is rebuilt in the same transaction. Two constants in `core/domain/Allowance`, for a premium tier to raise. Recorded as [`0015`](./0015-one-redo-a-fortnight-five-swaps-a-plan.md). (who decided: owner — the two allowances and their numbers; agent, the mechanism)
- 2026-09-09 — Generation asks each slot in its own request, in parallel, and covers a short first round from the library before asking the model again; the generation screen polls with backoff. A meal can be marked eaten or skipped. Recorded as [`0016`](./0016-one-round-per-slot-in-parallel.md). (who decided: owner, by report; agent, the mechanism)
- 2026-09-09 — Only an account with `email_verified` may use the product; the flag is set by hand while the provider is free-tier. A global guard after the session guard answers 409 `EMAIL_UNVERIFIED`, and the app sends the person to `/pendiente`. Recorded as [`0017`](./0017-access-opens-account-by-account.md). (who decided: owner)
- 2026-09-09 — The fortnight closes with a check-in, once per plan from its last day: the weight logs and the targets follow the latest weight; the portion answer moves the calorie target 5 % within the bounds; difficulty, satisfaction and their words go to the next plan's prompt. No answer is a restriction. Recorded as [`0018`](./0018-the-fortnight-closes-with-a-check-in.md). (who decided: owner, by request; agent, the mechanism)
- 2026-09-09 — The password-reset link is mailed over SMTP (nodemailer; four variables, all or nothing; Gmail with an app password until there is a domain) in the request's language, and the hook never throws so registered and unregistered addresses answer alike. Verification mail stays unsent on purpose: `email_verified` is the owner's switch (`0017`). Recorded as [`0019`](./0019-the-reset-link-travels-by-mail.md). (who decided: owner — real mail, Gmail as the sender; agent, the mechanism)
- 2026-09-09 — A progress screen reads what was logged and estimates nothing: the weight line on a time axis with start and target, and one card per fortnight lived with its meal marks (only days that have arrived) and its check-in. Adherence keeps the check-in's definition, eaten over marked. Recorded as [`0020`](./0020-progress-is-what-was-logged-shown.md). (who decided: owner — the next step; agent, the shape)
- 2026-09-09 — Earlier plans can be seen as they were and nothing about them can change: `/plan/historial` lists every plan lived, one opens in the same browser with marks shown and not offered, a meal of a closed plan has no controls, and the API refuses a mark on a plan that is no longer active as a conflict. Recorded as [`0021`](./0021-the-past-is-read-only.md). (who decided: owner — the next step; agent, the shape)
- 2026-09-09 — A swap may be asked for something: quicker, no cooking or more protein, each a filter on the library pick and on the model's answers alike, judged against the dish being replaced; cheaper and vegetarian wait for the data that could enforce them. Recorded as [`0022`](./0022-a-swap-can-be-asked-for-something.md). (who decided: owner — the next step; agent, the axes the data allows)
- 2026-09-09 — The end-to-end suites ran for the first time, against a throwaway database with a scripted model. They found that a free-text allergy excluded the matched row and not the food made of it: `madeOf` now excludes every catalogue row whose slug carries the anchor as whole tokens (`tomate` → `tomate-frito`, not `tomatillo`). Recorded as an amendment to [`0004`](./0004-deterministic-safety-layer.md). (who decided: agent, on evidence from the first run)
- 2026-09-09 — What a person will not eat is removed from the catalogue before the model sees it and from the library before reuse offers it: `ingredients.classes` records what a food is, `resolvePreferences` turns a way of eating and a dislike into excluded ids, and only what the catalogue could not resolve is still asked of the model. A user had written "pescado" in their dislikes and been served salmon. Recorded as [`0023`](./0023-a-preference-is-enforced-not-requested.md). (who decided: owner, by report; agent, the mechanism)
- 2026-09-09 — Six catalogue rows whose bare name left the form open are named for what a shopper buys: `Salmón fresco`, `Pavo fresco`, `Merluza fresca`, `Gambas frescas`, `Mantequilla con sal`, `Tomate fresco`. Slugs are unchanged, so nothing that matches on them moves. (who decided: owner, by report)
\n- 2026-09-09 — Failures reach the owner without waiting for a screenshot: 5xx responses and failed background jobs are reported to Sentry, off entirely without `SENTRY_DSN`, carrying the error, its stack and the route pattern and never a body, a user or a URL with ids in it. Recorded as [`0024`](./0024-a-failure-nobody-saw.md). (who decided: owner — the last item on the plan; agent, the mechanism)\n
- 2026-09-09 — An onboarding question must change something or it is not asked. An audit found five answers read by nobody: the day (bedtime, wake time, training days and time) now shapes the prompt, the cooking-time limit becomes a rule the pool and reuse both enforce, and the country is no longer asked. Recorded as [`0025`](./0025-every-answer-changes-something.md). (who decided: owner — "todos los datos del onboarding tienen que ser relevantes"; agent, the audit and the mechanism)
- 2026-09-09 — A swap can be asked for a vegetarian dish, on the food classes `0025` put in a column: no meat, fish or shellfish, eggs and dairy kept. The profile now says of each dislike whether it is kept out of the plan or only asked of the model, entry by entry, the way free-text allergies already do. Recorded as amendments to [`0022`](./0022-a-swap-can-be-asked-for-something.md) and [`0025`](./0025-every-answer-changes-something.md). (who decided: owner — the next two steps; agent, the mechanism)
- 2026-09-09 — What cannot be a rule becomes a weight: a dish from a kitchen they chose or using a food they said they like is offered by the library before the others, in the plan's rotation and in a swap alike, and nothing is excluded — so a narrow taste costs variety and never a plan. Recorded as [`0026`](./0026-a-preference-that-cannot-be-a-rule-is-a-weight.md). (who decided: owner — the next step; agent, the mechanism)
- 2026-09-09 — The green gate runs on a machine: `pnpm turbo lint ts:check test` on every push to `main` and every pull request. The deploy does not wait for it — the host builds whatever lands — so a red run is a record until a branch protection rule makes it a brake. (who decided: owner — the next step)
- 2026-09-09 — One mail a fortnight: a daily sweep tells accounts whose plan reached its last day that the check-in is waiting, writes the proof only after the provider accepted it, and carries no health data at all. The switch that stops it ships with it, on the profile. Recorded as [`0027`](./0027-one-mail-a-fortnight-and-a-switch.md). (who decided: owner — the next step; agent, the mechanism)
- 2026-09-09 — Every cron schedule is removed from `apps/api/vercel.json` while the project runs on free tiers: the two AI sweeps were already inert behind their own switches, and the check-in reminder — which spends nothing from the provider — went with them, because a scheduled call costs an invocation and one exception is harder to remember than a rule. The routes stay, guarded; §3b of the runbook says how to put one back. (who decided: owner)
- 2026-09-09 — The counters that stand between someone and unlimited password guesses move to the database: Better Auth's own limiter switches to `storage: 'database'` with a `rate_limit` table. The app's guard stays in memory on purpose — the paths that cost money are quota'd in Postgres already, and a write per request would buy nothing. Recorded as an amendment to [`0007`](./0007-own-the-rate-limiter.md).
- 2026-09-09 — Backups have a procedure and a script: `pnpm --filter database backup` writes one NDJSON per table plus a manifest naming the migration the data was shaped by, and §8 of the runbook says how the host's own point-in-time restore works and what neither covers. No restore script and no schedule, both said plainly rather than implied.
- 2026-09-09 — An admin screen at `/admin`, behind the stored role and 404 to everyone else, showing whether generation works and how big the catalogue is: counts, plans by state, and the last twenty-five generations with their failure codes. No user list, no plan viewer, no profile — those are how an admin surface becomes a way to read people's health data. Recorded as [`0028`](./0028-an-admin-screen-that-cannot-read-anyone.md). (who decided: owner — next on the roadmap; agent, the scope)
- 2026-09-09 — Every sign-up mails `OWNER_EMAIL` that an account is waiting, with the activation statement ready to paste; the hook swallows its own failures so a sign-up is never lost to a mailbox. It is the only mail that carries someone's address, because activation is by email. Recorded as [`0029`](./0029-the-owner-is-told-an-account-is-waiting.md). (who decided: owner, by request)
- 2026-09-09 — A confirmed address stops being a key: `activated_at` is the door and `email_verified` is the address, so the verification mail can finally be sent without handing out access. The owner's notice carries a signed one-click button, the admin screen shows the queue with one button each, and the error is renamed to say what it means. Recorded as [`0030`](./0030-a-confirmed-address-is-not-a-key.md). (who decided: owner, by request)
- 2026-09-09 — Two switches, not one: the door (`app_settings.registration_open`, thrown from `/admin`, refusing sign-up with a 403 the form explains) and the key (`activated_at`, an admin's act). Registration is open by default; a missing settings row must not shut a service. Recorded as [`0031`](./0031-the-door-and-the-key-are-two-switches.md). (who decided: owner, by request)
- 2026-09-09 — The web app's gates were still asking `emailVerified` after the split, so a confirmed-but-unopened account reached the app shell and `/pendiente` bounced it back to `/inicio`. `UserView` now carries `activated` and both gates read it. Found in production, not by a test. Recorded as an amendment to [`0030`](./0030-a-confirmed-address-is-not-a-key.md). (who decided: owner reported it)
- 2026-09-09 — The door now says who turns the key: with registration open, confirming the address writes `activated_at` and the person is in; closed, it confirms the address and an admin opens the account. Read at the click, never failing the verification. Recorded as an amendment to [`0031`](./0031-the-door-and-the-key-are-two-switches.md). (who decided: owner, by request)
- 2026-09-09 — Both locks, and one place to see them: the guard now refuses on either an unconfirmed address (409 `EMAIL_NOT_VERIFIED`) or an unopened account, `/pendiente` says which of the three waits it is, the owner's notice moved from sign-up to the moment an account actually lands in the queue, and `/admin` lists every account with the state of both. Recorded as a second amendment to [`0031`](./0031-the-door-and-the-key-are-two-switches.md). (who decided: owner, by request)
- 2026-09-09 — The door is retired. Sign-up is never refused; the one switch decides whether confirming an address opens the account (automatic) or leaves it in the owner's queue (manual), and it is renamed `automatic_activation` in code and database because it had stopped governing registration. Recorded as a third amendment to [`0031`](./0031-the-door-and-the-key-are-two-switches.md). (who decided: owner, choosing between two readings)
- 2026-09-09 — Vacation mode is a pause, not a second plan: declaring a trip moves every plan day at or after it forward by its length, so those dates hold no meals, nothing counts as skipped, and the check-in mail moves with the plan's last day without being taught about holidays. Cancelling gives back only the days not yet spent. Recorded as [`0032`](./0032-a-pause-moves-the-plan.md). (who decided: owner, choosing the pause over a generated holiday plan)
- 2026-09-10 — Analytics counts the rows and logs only what leaves none: an eight-stage funnel computed from state, so it is right retroactively and cannot disagree with the data, plus exactly two events for the things nothing records — a session started (sessions expire and vanish) and the axis a swap was asked for. No client-side writer, no content in an event, counts on the screen and never people. Recorded as [`0033`](./0033-count-the-rows-log-only-what-leaves-none.md). (who decided: owner — next on the roadmap; agent, the split)
- 2026-09-10 — A plan is built from what you can buy: `ingredients.countries` (empty means everywhere; thirty rows of nine hundred name `ES`), the catalogue query drops what is sold only elsewhere, and onboarding finally asks the question now that something reads it. A country nobody stated filters nothing. English alongside Spanish was found already finished. Recorded as [`0034`](./0034-a-plan-is-built-from-what-you-can-buy.md). (who decided: owner — next on the roadmap; agent, the shape)
- 2026-09-10 — Two people's plans differ because the scheduler says so, not because the model wrote enough dishes to hide the overlap: `pickBest` breaks a tie by the pool's own order — that user's seeded shuffle — instead of alphabetically, and the library fills a slot's fourteen days instead of twelve. A plan the gate refuses is retried once with the whole library before the generation fails, which costs no call. Recorded as amendments to [`0013`](./0013-a-third-of-every-plan-is-fresh.md) and [`0009`](./0009-rotate-reuse-per-user.md). (who decided: owner, from two user reports)
- 2026-09-10 — `/admin` counts the provider requests that leave this service and measures them against a limit the operator configured, saying in its own copy that those are two different things — Google publishes no remaining-quota endpoint, and the binding constraint turned out to be twenty requests a day rather than tokens. The account list is paged and newest first: capped at two hundred oldest-first, it showed the founders and lost everybody who needed something. Recorded as [`0035`](./0035-the-panel-counts-our-own-calls.md). (who decided: owner, by request)
- 2026-09-10 — Two bugs from a phone. A paused plan is read-only and the controller is what says so — marking, swapping and verdicts answer 409 `PLAN_PAUSED` — because a disabled button is a suggestion. And the shopping list stopped being a snapshot: `shopping_list_items.name` was frozen the day the list was built, so switching language kept a Spanish list forever; names now resolve against the catalogue in the reader's language, with the stored name surviving as the fallback for anything hand-added. Recorded as an amendment to [`0032`](./0032-a-pause-moves-the-plan.md).
- 2026-09-10 — A profile says which meals somebody eats and how big each one is, replacing a count that could not name them: `slotsFor` took the first N of breakfast, lunch, dinner, so "two meals" always dropped dinner and somebody who skips breakfast had no way to say so. Four sizes a slot, weights underneath, and the scheduler unchanged because it already normalised over whatever weights it was handed. The backfill reproduces the old derivation exactly, so nobody is asked again. Recorded as [`0036`](./0036-which-meals-and-how-big.md). (who decided: owner, by request)
- 2026-09-10 — A box for what people think: a signed-in person writes to the owner, who gets an inbox with a reversible "handled" mark. The one admin read that carries somebody's words and address, because the message was written to be read and answered — nothing summarises it and it never reaches a model. Recorded as [`0037`](./0037-a-box-for-what-people-think.md). (who decided: owner, by request)
- 2026-09-10 — A tour of what is already here: five stops on the dashboard, shown once, each a title, a sentence and a link — the fortnight is planned, a dish can be changed along an axis, the meals of a day can be shaped, a trip pauses the plan, and the check-in sets the next one. The shopping list and the progress chart are left out because they are in the navigation bar. The mark is `profiles.tour_seen_at` rather than browser storage, so it does not reappear on a second device, and null means never — which is every account that existed before the tour did. Recorded as [`0038`](./0038-a-tour-of-what-is-already-here.md). (who decided: owner, by request; agent, the five stops)
- 2026-09-10 — Every API module gets the same five parts — module, `controllers/`, `services/`, `dto/in`, `dto/out` — and a route names what it takes and what it answers instead of returning whatever a literal happened to hold. The DTO question is settled without a second definition of any rule: a body DTO names the Zod schema in `packages/core/entities` that the web form already shares, and `@ZodBody` binds the pipe, the parameter type and the generated OpenAPI schema together. Responses stay status plus prose, because a hand-written response class is a copy of a core presenter that nothing checks. Recorded as [`0039`](./0039-a-route-names-what-it-takes-and-what-it-answers.md). (who decided: owner — the reference backend's shape; agent, the reconciliation with Zod)
- 2026-09-10 — One address per language: Spanish stays at the root, English moves to `/en`, and each language is its own root layout — so `<html lang>` is a literal, twelve public pages prerender instead of running on a function per request, and the English half of the product can be indexed at all, which it could not before. With it, everything a crawler was missing: `robots.txt`, `sitemap.xml`, canonicals and `hreflang`, a title per route, Open Graph with a generated image per language, and structured data built from the same dictionary the page renders. The cost, paid knowingly: first-visit `Accept-Language` detection is gone from public pages, because reading that header is exactly what made them dynamic. Recorded as [`0040`](./0040-one-address-per-language.md). (who decided: owner, choosing option B from three; agent, the three-root-layout mechanism)
- 2026-09-10 — What a page owes a reader who cannot see it: twenty-five WCAG 2.2 AA defects fixed, three of them decisions — a `--border-interactive` token at 3:1 because a control identified by a 1.26:1 edge is a control you hunt for; `--foreground-03` collapsed onto `--foreground-02` because on this ground **no grey is both legible and lighter than the secondary tier**, so the third tier survives as a name and dies as a colour; and topics under a section became headings at the size they already were. Also: the tour announced itself as an unnamed dialog and opened with focus on its own exit, and plan generation's live region did not exist in the branch that renders a failure. Recorded as [`0041`](./0041-what-a-page-owes-a-reader-who-cannot-see-it.md). (who decided: owner, by request; agent, the three trades)
- 2026-09-10 — What a paid account may spend, and one switch that decides the tier exists at all. The switches become a registry (`core/domain/Flag`) where every flag declares its row key, **which way it fails when no row exists** — `automaticActivation` on, because queueing everybody over a missing row fails shut for the wrong reason; `premium` off, because a missing row must never put a price in front of somebody — and who may read it, since an operational switch is nobody's business but the owner's. The tier itself is `user.tier`, granted by the owner, and the flag outranks the column: with `premium` off everybody is on the free allowances whatever their row says, so turning the tier off is one click rather than a migration. Premium raises redos from one a fortnight to three and swaps from five to twenty; nothing about safety, allergies or plan quality is behind it, and nothing ever will be. Payment capture is deliberately absent — it cannot be tested without a processor account, and an untestable payment path is the partial state this codebase refuses everywhere else. Recorded as [`0042`](./0042-what-a-paid-account-may-spend.md). (who decided: owner, by request; agent, the flag-over-column ordering)
- 2026-09-10 — A day that eats for something: an event — a race, a match, a Hyrox, a long session, named by the person — and the one to three days before it that eat for it. The person chooses the shape (which macros go up, down or stay) and never the size: `LOAD_STEP` is a fifth, one constant, on no screen, because the number is where the harm would live. A loaded day's targets are held to the same bounds as the profile's and a refused load is recorded rather than applied; there is deliberately no refusal keyed on a condition, per `0008`'s rule that this product does not write prescriptions. The plan day stores what it was built to and why, because the event may be deleted and the plan is history. Applied at the next generation — the scheduler gains a per-day override map and with none produces the same fortnight it always did. Recorded as [`0043`](./0043-a-day-that-eats-for-something.md). (who decided: owner, from a user's request; agent, size-in-code and the deferral of in-place rescheduling)
- 2026-09-10 — A fortnight rebuilt for an event: the in-place rescheduling `0043` deferred, premium only, plus a cap on events per fortnight (free three, premium ten; `429 QUOTA_EXCEEDED`, `kind: event`). Premium gets a second allowance, three mid-plan events a plan, and spending one rebuilds only the loaded days *strictly after today* of the active plan — from the library, never the model; through the allergy gate again; with the variety rules held against the days kept; the shopping list rebuilt; all in one transaction with the counter, which is a column on the plan (`mid_plan_loads`) because a count derived from `plan_days` cannot tell a load applied at generation from one applied mid-plan. Meals are updated in place so `meal_swaps` never cascades away. Every reason not to rebuild answers the same way — an empty `rebuiltDates`, the event waits for the next generation. Recorded as [`0044`](./0044-a-fortnight-rebuilt-for-an-event.md). (who decided: owner, the caps and the premium rebuild; agent, the counter's home, in-place meals and today-is-not-rebuilt)
- 2026-09-10 — Events move off the profile: an event is a fortnight's, not the person's, so it is declared where the fortnight is made — on the generation screen before a plan is built, and under a day of the plan for the paid tier's mid-plan addition. The generation screen stops starting on arrival, one more tap per regeneration, accepted. Recorded as an amendment to [`0043`](./0043-a-day-that-eats-for-something.md). (who decided: owner)
- 2026-09-11 — A day is built to all four of its numbers. The scheduler's fit function compared energy and protein only, so every day of a real plan missed carbohydrate by up to 46% and fat by 75% while hitting the other two to the gram, and no band existed to notice. Carbs and fat join the fit; portions are sized by exhaustive search instead of a greedy walk; a swap is judged by the day it becomes once re-sized; and validation holds all four macros to 5%, as guidance rather than rejection. Measured on the owner's own profile against the real library: fourteen days of fourteen inside 5% on all four, event days inside 1.3% of their raised targets, four seconds a fortnight. Recorded as [`0045`](./0045-a-day-is-built-to-all-four-of-its-numbers.md). (who decided: owner — "perfect, or it is no use"; agent, the mechanism and the advisory-not-blocking call)
- 2026-09-11 — A plan that misses its macros is scheduled again, from a wider rotation. `0045` measured fourteen of fourteen days inside 5% against the whole library, but generation hands the scheduler a dozen rotated dishes per slot (held short for `0013`'s fresh third), and the dishes carrying the carbohydrate were spent in the first week: days ten to fourteen of the owner's regenerated plan missed fat by up to 37%. When a plan misses a band, it is now scheduled once more from the pool it had plus the rest of the user's rotation, uncapped — same shuffled order, no last-fortnight dishes, no dislikes, the model's fresh dishes first, no second model call — and the plan that misses by less is kept. Reproduced on the exact rotation: a dozen per slot, fat 50% at worst; uncapped, fourteen of fourteen inside 5% on all four. Recorded as [`0046`](./0046-a-plan-that-misses-its-macros-is-scheduled-again.md). (who decided: owner — "analiza si está bien"; agent, the mechanism)
- 2026-09-11 — The full-library rescue no longer serves a disliked dish. It read the library with no rotation, so the path meant to spare somebody an empty fortnight could hand them a plate they had refused (`0014`). Asked whether the model could write a replacement instead: it mostly cannot — the rescue runs when the model has just failed, and the first pass already asked it for the shortfall. Dislikes are excluded, no extra call; last fortnight's dishes may still return; if nothing is left the person gets the honest error. Recorded as an amendment to [`0046`](./0046-a-plan-that-misses-its-macros-is-scheduled-again.md). (who decided: owner)
- 2026-09-11 — Dishes are designed to the whole split, per serving, at the person's own size. A second account's plan missed carbohydrate and fat on most days because its library had no dishes at its split: the prompt briefed every dish at the whole day's size (a share computed over the one slot in each request since `0016`), never mentioned carbohydrate or fat, and still claimed a plan short on protein is discarded. The prompt (3.0.0) now briefs each slot per serving on all four macros and fibre from the person's meal shape, states the split, teaches how a plate reaches it, adds plate guidance per goal and a share of dishes at any event day's split, and puts way of eating with allergy at the top. Recipes are stamped with a separate steps version (held at 2.8.0) so the change does not send the whole library through a model rewrite. Effect measurable only by regenerating. Recorded as [`0047`](./0047-dishes-are-designed-to-the-whole-split.md). (who decided: owner — "super profesional, nivel experto… para todo tipo de personas y objetivos"; agent, the brief and the version split)
- 2026-09-11 — Protein is held both ways, and the fortnight is repaired as a whole. A real 3.0.0 regeneration landed energy, carbohydrate and fat inside 5% every day and protein 7–17% over on nine, unreported: protein had only a floor and a safety ceiling. Validation now reports protein more than 5% over (advisory; the 3 g/kg ceiling still blocks) and counts it towards `0046`'s second schedule. The scheduler gains a pass after every day is built: days outside a band are re-sized to the bands, then trade a meal with the same meal of another day while that leaves fewer macros outside. The prompt (3.1.0) asks for protein as a figure to land on and for each meal's dishes to straddle it, as numbers. A penalty inside the day's own build, pool replacements in the pass and composition-picked rotation were measured and rejected. Measured on a production copy: the reconstructed pools reach 14/14 on all four; a real regeneration landed all four inside 5% on all fourteen days, events included. Recorded as [`0048`](./0048-protein-is-held-both-ways.md). (who decided: owner — "fixea también la proteína"; agent, the mechanism)
- 2026-09-11 — Generation reads the whole library, and a day's portion search has a ceiling. Loading a five-hundred-dish seed into development showed `findReusable` read 300 recipes with no order — a sample, not the library — and that a six-meal fortnight took 98 seconds to schedule in production code, against a 300-second function. The whole library is now read, in slug order; a day prices at most 59,049 portion combinations (what five meals already cost), six meals narrowing their lighter meals first; the spread pass prices at most a four-meal day's search. Six meals: 98 s → 29 s. Recorded as [`0049`](./0049-generation-reads-the-whole-library.md). (who decided: owner — the seed, dev first; agent, the read and the ceiling)
- 2026-09-12 — Generation can go through an OpenAI-compatible gateway. `AI_PROVIDER=omniroute` sends the pool builder's calls to OmniRoute at `AI_BASE_URL`, with `OMNIROUTE_API_KEY` and `OMNIROUTE_MODEL` (default the `NutrIA-Fallback` combo) beside each other in the environment. Structured outputs travel non-strict — strict mode made a model answer 400 to every request — a response that parses into the wrong shape is a provider error rather than a crash, and the SDK does not retry behind a gateway that already fails over. Measured on a demanding weekly benchmark: `muse-spark` and the combo keep 7/7 days within 5% on all four macros alongside the library, with no unsafe meal. Open before production uses it: nothing bounds a model call inside the 300-second function. Recorded as [`0050`](./0050-generation-can-go-through-a-gateway.md). (who decided: owner — "conéctalo a OmniRoute… el model se tiene que configurar por env"; agent, the wiring)
- 2026-09-12 — The pool prompt names an ingredient only where its slug does not already say it (3.2.0), and a slug returned with accents is read in the catalogue's spelling (3.2.1). The list was 70% of the prompt and 525 of 570 names repeated their slug with the accents back; input per call fell by a third (8.3k → 5.6k tokens) with every ingredient still offered. A model shown bare slugs wrote some back as "brócoli" or "calabacín", rejected as unknown; every catalogue slug is lowercase ASCII, so the dish schema folds accents before the lookup and the allergy gate runs on what it resolves to. Filtering the list by what recipes already use would save more (−39% for a person without restrictions) and was left out: an ingredient no recipe uses would never be offered again. (who decided: owner — "prueba la opción A", "arregla lo de las tildes… la versión será 3.2.1"; agent, the rule)
- 2026-09-12 — Every generation keeps a log of its model calls, and the admin reads it. Each call records the model asked for and the one that answered, the provider, our clock and the gateway's, the tokens with reasoning and cache, the cost, the gateway's request, correlation and combo ids, the session it was filed under — the job id, sent as `x-omniroute-session` to a gateway and to nobody else — what became of each dish, and for a failure its status and the quota Google writes into a refusal. It is written to the job row (`plan_generation_jobs.ai_calls`) as soon as the pool is built, so a generation that fails afterwards keeps it, and each call is one line in the server log. The admin gains the latest twenty generations with the address that asked — on a controller of its own, since it carries a person (`0028`) — and today's calls by the model that answered, with the last refusal and its limit. Not in it: OmniRoute's own limits and resets per model, which only its management API has, behind a key with the `manage` scope. (who decided: owner — "todos los datos que me pueda proporcionar omni… que usuario la ha realizado"; agent, the fields and where they live)
- 2026-09-12 — The model half of a generation has a time budget. `vercel.json` gives a generation 300 seconds in one function and nothing bounded a model call: a combo that failed hop after hop once took 645. Every round of calls now shares one budget — `AI_BUDGET_SECONDS`, 170 by default, which leaves room to schedule and save in the same invocation. The first round has all of it, a later one starts only with 15 seconds left, and a call still waiting at the end is cut and logged as a `timeout`; the library covers what the model did not bring, and the plan's metadata says the budget ran out. A host with no function limit raises it. This closes the open risk `0050` named. (who decided: owner — "haz 1 y 2"; agent, the figure and the rule)
- 2026-09-12 — A dish's time is held to the person's limit plus a fifth, rounded up to the next ten minutes: 30 admits 40, 55 admits 70, 25 admits 30. The minutes on a dish are an estimate — the model's or the recipe's — and the strict limit dropped dishes for running a minute or two over: time was a routine rejection across the week's benchmark, and a real generation lost its only model dinner to it. One function (`timeAllowance`, inside `withinTime`) decides it for the model's dishes, the library and swaps alike; the prompt still asks for the limit itself, so most dishes land inside it and the margin only saves the near misses. There is no lower bound — a quicker dish was always fine. (who decided: owner — "un 20% más, y redondea siempre para arriba… 34, pues 40; 66, pues 70")
- 2026-09-12 — A generated dish may carry fifteen ingredients, eight servings and six-hundred-character steps (prompt 3.2.2). A real regeneration lost its whole lunch: all seven dishes had thirteen to fifteen ingredients against a schema ceiling of twelve, while the prompt said "fifteen is a shopping trip". Validating every response the gateway kept that day: 77 of 628 dishes failed the schema — 41 on ingredients (none over fifteen), 11 on servings (all six), 10 on a step's text (404–561 characters), 17 on the method floor. The first three ceilings now match what `candidateDishSchema` stores; the prompt states the ingredient ceiling and the servings, seasonings included. The method floor stays: a dish that does not say how to make it is a quality rejection, not a limit. The step rewrite takes six-hundred-character steps too. (who decided: owner — "sí, y optimiza las raciones y los pasos"; agent, the ceilings)
- 2026-09-12 — A model call is abandoned at its budget whatever the transport does, and a generation fails itself before the function ends. The first regeneration in production through the gateway never finished: three calls answered in 63–89 seconds, a fourth never did, and the platform's log shows it still waiting when the function was killed at 300 s — its aborted signal had not ended the request there, though the same abort cut a call at 8 s against the same gateway from a workstation. The job sat `running`, the screen waited on it, and no plan was saved. The client now races each call against its signal as well as handing the signal on, so a call past its budget is given up at once and the library covers the rest; the job runner fails a generation still running at 280 s with `GENERATION_TIMED_OUT` (its own copy, retryable), a late stage cannot mark it running again, and a generation past that point does not save. Each round's slots and remaining budget are now one log line. (who decided: owner — the report of a frozen screen; agent, the diagnosis and the two nets)
- 2026-09-12 — A day keeps its shape and its proteins apart (`0051`). The first plan through the gateway was inside 5% on every macro every day and still read wrong: a 388-kcal lunch beside a 1,247-kcal dinner, tuna in nine meals, egg whites three times in a day, one salad on two consecutive days, pasta at breakfast, rice skewers as a snack. Each meal is now held near its share of the day (0.7–1.4, priced by the distance, below every macro), a main protein is served once a day and in about one meal in ten (priced, not refused), a dish never comes back the same day or the next, breakfast and snacks are told what kind of food they are (prompt 3.3.0), and seven plated seed dishes stop being snacks (migration `0026`). Measured on seven profiles: macros 98/98 on all four before and after; meals outside their share 70 → 0, days with a repeated protein 30 → 0, back-to-back dishes 7 → 0. (who decided: owner — "ataca estos 4 puntos"; agent, the rules, the weights and the numbers)
- 2026-09-12 — Protein powder is for the people who take it (`0052`). The next plan used whey or pea protein in seven breakfasts of fourteen, for somebody who records no supplement, and the supplements form asked every supplement — creatine included — for its protein. A supplement now has a kind; protein grams belong to the protein kind only (migration `0027` back-fills the ones with grams); and five protein-supplement rows leave the catalogue of anybody who records no protein supplement, excluded like a dislike. Generation reads only whether a protein supplement exists, in `core`; the model never sees it. A main protein is also held to three times in any one meal of the fortnight. (who decided: owner — "proteína en polvo, solo para quien la use… debería estar marcado en suplementos", "limita una misma proteína a 3 veces"; agent, the kinds, the list and the rule)
- 2026-09-12 — The rewrite sweep runs again, daily, through the gateway and bounded in time. It was off since 2026-09-09 because on Google's free tier it would have spent the daily cap generation needs in two hours; through the gateway its free models carry it. Measured there: 22–74 seconds a rewrite (432–637 tokens in, 2,000–8,500 out, nearly all reasoning), against 71–180 for a generation call — but ten in a row, as the sweep ran them, would outlive the 300-second function. So a sweep now runs three calls at a time, starts none with less than 90 seconds left, ends every call by 240 seconds with the same race generation uses (`7d56f3a`), fetches twelve, and logs each call. `AI_REWRITE_MODEL` can point it at a combo without the Gemini step. The cron entry is back in `vercel.json` for this route only, still behind `AI_REWRITE_STEPS`. (who decided: owner — "volver al cron de rewrite steps"; agent, the lanes, the deadline and the batch)
- 2026-09-12 — A rewritten method is written like a professional's and read back before it is stored. The rewrite prompt now carries the generation prompt's standard — the cut, the vessel, the heat, the time, the sign it is done, food safety where it matters — and one rule above all of it: the ingredient list is the whole kitchen, every ingredient named where it goes in and nothing else added, not a pinch of salt, not "serve with bread", water aside; the model is told why (the list was checked against somebody's allergies, the prose was not) and that quantities stay out of the steps, since the list beside them is scaled to each portion. Then `methodMentions` reads the answer back against the catalogue in its language: a method that names a food the dish does not contain, or never says where one of its own goes, is refused and the old one kept, as is one with a step about its own brief — a real rewrite closed a cottage-cheese cup certifying that "all three listed ingredients are named". The prose is tidied before it is stored: an ingredient's catalogue capital dropped inside a sentence, a field's name or a zero-minute clause echoed into the text removed. A curated handful of foods whose names are also a cook's words — "dorada", "rosada", "cuajada", "mango", "pasas", "gallo" — are not counted as foreign, nor is a part of a listed food ("pechuga de pavo" in a dish of "Pavo", "zumo de naranja" in one of "Naranja") or the dish's own name ("el arroz negro"). Run read-only over all 778 stored methods in production, the first version flagged 75, 43 of them for an egg white "cuajada" — set, not curd — which would have refused almost every egg dish the sweep rewrote; the final one flags 20, and none adds an allergen its dish did not already carry: salt in eleven, "lemon if you like" in two, honey, a fish stock in a fish rice, and a few borderline wordings. Four rounds of three real rewrites through the gateway, prompt adjusted between them, ended with all three valid, nothing foreign, nothing missing: 19–65 seconds a call, about a thousand tokens in. `STEPS_VERSION` is unchanged, so the five hundred seed dishes are not queued again. (who decided: owner — "recetas perfectas, con todos los ingredientes que da el plato, sin nada más, comprobar alérgenos por si acaso"; agent, the prompt and the read-back)
- 2026-09-12 — A sweep holds what it takes, a generated dish is read back like a rewritten one, and the flagged library is written again. Pressing "Run" twice started two sweeps on the same twelve recipes; the pick is now one `UPDATE … RETURNING` over a subquery locked `FOR UPDATE SKIP LOCKED`, which stamps `steps_claimed_until` five minutes ahead (migration `0028`), so a second sweep takes the next twelve and a claim its sweep could not finish lapses by itself. `PoolBuilder` now refuses a generated dish whose method names a food its ingredients do not have (`foreign_food`, on `/admin`) — 5 of 278 stored AI dishes did — but not one whose steps leave an ingredient unnamed, which harms nobody and would have cost 37 of the 278. The twenty stored methods the read-back flagged in production lose their stamp (migration `0029`, by slug) and are rewritten under the new rules. (who decided: owner — "va, empieza"; agent, the claim, the rule and the list)
- 2026-09-12 — The shopping list survives the supermarket (`0053`). The web app could already be installed (manifest, icons); a service worker written by hand now keeps the last copy of `/inicio` and `/compra` — the HTML and the static files it names — going to the network first and to the copy only when there is none, and opening on today's copy when the installed app starts offline. The page refreshes both copies on every change of screen (once a minute at most) and as the app is put away; any redirect from those pages drops them, and so do sign-in, sign-up, sign-out and account deletion. A screen read from a copy says what time it is from and that ticks are not saved. Checked in headless Chrome against a production build and a throwaway dev account, deleted afterwards: stored after a client-side sign-in, opened with the web server stopped, a client navigation offline falls back to the copy, the app opens on today, `/perfil` is left alone, signing out empties the copies. Web push for the check-in is the rest of roadmap 8.1. (who decided: owner — "va, empieza", block 3; agent, the two screens and the worker)
- 2026-09-13 — The first visit stores the copies too (`0053`). The first try on an iPhone opened the site with a connection, turned on airplane mode without changing screen, and got Safari's "not connected" page: nothing had been stored. On that first visit the page loads before the worker exists, so nothing controls it, and the refresh the page asked for went to `navigator.serviceWorker.controller`, which was null, and was lost. It now goes to `navigator.serviceWorker.ready`, which waits for the worker to be active whether or not it controls the page. Reproduced in headless Chrome against the build before the fix (nothing stored; `/inicio`, `/compra` and `/` all failed offline), then fixed (both copies stored without changing screen; all three opened offline). (who decided: agent, from the owner's screenshot)
- 2026-09-13 — The plan and today's recipes are kept offline too, and the menu only offers what opens (`0053`, amended). The owner asked for every screen offline, or at least a menu that shows only what works offline. Now kept: `/plan` and meals' own pages. A meal page is kept once read online, and each refresh fetches the pages of today's meals once each; pages of meals no longer in the plan are dropped. Offline, or when reading a copy, the menu shows only the screens with a copy and hides the language switch and signing out, and a meal without a copy is not a link. `/perfil`, `/progreso` and `/check-in` stay out: they hold health data, and they are forms that cannot save offline. (who decided: owner, "todas las pantallas… o al menos el menú"; agent, which screens and why not the rest)
- 2026-09-13 — The check-in reminder comes back, behind a switch, and reaches the phone too (`0054`). The reminder of `0027` had not been scheduled since 2026-09-09. The cron is back, daily at 08:00 UTC, and a new owner-only switch on `/admin` (`checkInReminders`, off when there is no row) decides whether anything goes out at all. Push is a second channel: a browser subscribes from the profile (`push_subscriptions`, migration `0030`). The sweep tries mail and push each on its own, and one `notifications` row records whichever carried the reminder; "already sent" counts any channel. Endpoints are accepted only on the push services browsers use, the message says nothing about health, and a tap only ever opens a page of this site. A subscription ends at sign-out, at account deletion, and when its push service answers 404 or 410. VAPID keys are all three or none; with none, the reminder goes by mail only. (who decided: owner — "haz el paso 1, pero pon una feature flag en admin para activar y desactivar"; agent, one switch for both channels and the endpoint allowlist)
- 2026-09-13 — A test notification from `/admin` (`0054`). "Enviarme un aviso de prueba" sends the reminder's own words, marked "Prueba", to the browsers of the owner who pressed it, and nobody else's. It works whatever the reminder switch says and records nothing. The answer tells apart four cases: push not set up, no device subscribed, every device refused, and sent. Owner-only (`POST /admin/push-test`), ten an hour. (who decided: owner — "añade el botón de aviso de prueba"; agent, what it answers)
- 2026-09-13 — A tick in the aisle waits for the signal (`0055`). Offline, a tick on the shopping list used to undo itself. Now every tick is queued on the device (`localStorage`, one entry per item, the last tick wins) and sent at once. With no connection it stays ticked, and it is sent again on opening a signed-in screen, on reconnecting and on returning to the app. A 4xx drops it and puts the item back; no connection or a 5xx keeps it. The item and the "X de Y" count show what the device knows. The queue is cleared at every change of session, and between two devices the last tick to reach the server wins. Only the shopping list works offline; the notice now says the rest needs a connection. (who decided: owner — "va, continúa"; agent, the queue, last-write-wins and what counts as a refusal)
- 2026-09-13 — Premium is paid through Stripe, and the owner tries it first (`0056`). Checkout and the Customer Portal are Stripe's own pages; the API only hands out their addresses. The signed webhook (raw body, 404 when the signature fails) fetches the subscription from Stripe and writes `subscriptions` (migration `0031`) and `user.tier` in one transaction. `active`, `trialing` and `past_due` pay; anything else grants nothing. With test keys only the owner sees billing, whatever the switch says; with live keys everybody does, once `premium` is on. `STRIPE_*` is all three or none, and a live key is refused on a preview. The profile gets a premium card. (who decided: owner — "continúa con el premium"; agent, test keys owner-only, the status rule, fetching over trusting the event)
- 2026-09-13 — The shopping list can be sent, and the nearest shops are a tap away. "Compartir lo que falta" sends what is left to buy, grouped by aisle with quantities. It uses the system share sheet, or the clipboard where there is none, and it counts this device's ticks, including those still waiting for a connection. "Supermercados cerca" opens Apple Maps on Apple devices and Google Maps elsewhere, searching around wherever the phone is: the map knows the place, and this app never asks. The map link is hidden offline. Affiliate links were weighed and left out for now. No Spanish supermarket lets another app fill a basket. Carrefour's programme (Awin) pays on a basket the person builds themselves. Amazon's pre-filled cart needs a product chosen for every ingredient and sells little fresh food. (who decided: owner — "compartir lista y súper cercano"; agent, what is shared and which map)
- 2026-09-13 — A free week, and a yearly price (`0056`, amended). Checkout opens with a seven-day trial (`TRIAL_DAYS`) for somebody who has never subscribed. Stripe takes the card at the start and charges when the trial ends. Somebody who subscribed and cancelled gets no second trial, so trials cannot be chained into a free tier. `STRIPE_YEARLY_PRICE_ID`, optional, adds a yearly price; the card then offers monthly and yearly, with what the year saves. Checkout takes `{ plan }`, and asking for yearly when none is set is a 404. A trial shows on the card as "en tu prueba gratuita hasta el…". (who decided: owner — "haz el paso 1"; agent, one trial per account and the saving shown)
- 2026-09-13 — The interface follows the HIG (`0057`). The owner installed a design skill built on Apple's guidelines, asked for an audit and then for every finding fixed. One control vocabulary: `Button` exports `buttonClassName`, so `CtaLink`, the pager and the map link wear the same classes; four variants, a toggle is a secondary with `aria-pressed`. A fill that carries a white label in both schemes (`--color-brand-fill`, 4.8:1) replaces step 09 under every filled control — in dark mode white on step 09 measured 3.9:1 on every primary button. Semantic radii (10/14/20), one `Card` component in place of sixteen copied recipes, 44px targets under `(pointer: coarse)` on every control and standalone link, the bottom bar's tab also semibold, the profile as a five-section settings list, the premium card's 1.8:1 error text fixed, no hover lifts, the tour a bottom sheet on a phone. (who decided: owner — "arregla todo lo que dices… que se vea como Apple", "skill de refactor"; agent, the fill token over darkening the scale, growing the control over a hit area)
- 2026-09-21 — Somebody can arrive through Google or Apple (`0058`). Each provider exists when its credentials do (`GOOGLE_OAUTH_*`, `APPLE_OAUTH_*`), whole or absent, and none by default, so nothing changes until the owner sets them. Apple's client secret is signed at boot from the developer key, so nothing expires. An account born through a provider has its address confirmed already, so the verification hook never runs for it: `onAccountCreated` opens it when registration is open and tells the owner when it is not. Linking is on only while a provider is, and only into an account that has confirmed its address, with no provider listed as trusted — otherwise signing up with a stranger's address would be a way into their account the day they arrived through Google; refused, the sign-in page says to use the password or reset it, and a reset now ends every session. `GET /settings/sign-in-providers` is public and the web app reads it on the server, kept five minutes, so the pages stay static and the buttons are in the first paint. Buttons are secondary and equal; Google's mark keeps its colours, Apple's takes the label's. Microsoft is a registry entry away; Facebook was left out. (who decided: owner — "estaría bien poder hacerlo con google, con icloud"; agent, the linking rule, the signed secret and the public list)
- 2026-09-21 — The legal pages exist: a privacy policy and terms of use, in both languages. Google refuses to publish a sign-in app without a privacy policy address, so `/privacidad` went out with `0058`; `/condiciones` followed the same day. One screen (`LegalScreen`) prints either document from the dictionary, the footer has a Legal column again, and a sentence under the sign-in and sign-up buttons links both — on sign-in too, because "Continue with Google" there creates an account. Both are written from what the code does: allergies reach the AI provider and conditions, medication and supplements never do; deleting the account deletes everything; two cookies and no third-party one; Premium renews, cancels from the profile and keeps access to the end of the paid period. The controller and provider is the owner as a private individual. The terms give the 14-day withdrawal in full from the first charge rather than the waiver, since checkout records no consent to one. A change to what is collected, to what reaches the model or to how Premium works is now a change to these pages. (who decided: owner — "escribo una página real ahora", "podemos crear también la pantalla de condiciones de uso"; agent, the wording, one screen for both and the full withdrawal right)
- 2026-09-21 — Two skills for what was retyped every time: `/ship` and `/local-probe`. `/ship` is owner-invoked only (`disable-model-invocation`), so typing it is the go-ahead to merge; its gate adds `check:leaks` to what CI runs, because the pattern list is gitignored and CI cannot — the day it was skipped, a name and an address reached a public repository. It waits for production on the deployments Vercel reports to GitHub against the merged commit, not on a URL answering 200, which proves a new route and nothing about a changed one; the API lands about a minute after the web app. `/local-probe` starts the API before building the web app (the sign-in pages ask it which buttons to draw while they prerender), compares the database host with production's and prints neither, empties every mail variable, gives each page a fresh browser context (a shared one carried the language cookie into the next page and the proxy redirected it), and measures a touch target by what a finger hits — a wrapping label, a pseudo-element that grows it — not by what is painted. Its first run found two date inputs at 40 px on a touch viewport, in the holiday planner. (who decided: owner — "monta la skill ship y el local-probe"; agent, the deployment signal, the guard and the target measurement)
- 2026-09-21 — A field's minimum height is stated once, and every change to what somebody sees goes through the design review. `Input` had two minimums: `min-block-size: var(--target-min)` under `(pointer: coarse)`, and `min-height: var(--height-l)` on the date and time types, which is the same property and the more specific rule — so seven fields (holidays, events, the birth date and three times in onboarding) stayed at 40px on a phone after `0057` said 44. Both rules now read one `--input-floor`. Measured in Chrome: 44px on a touch viewport, 40px under a pointer. The probe that found it also counted a switch inside its 44px label as 25px; it now treats any control inside a `<label>` as the label's height. And the owner's `apple-web-design` skill, which was only named in a private instruction file, is now a rule in `apps/web/AGENTS.md` § Design review, pointed to from the root and `packages/ui`, and checked by `/ship`: review what you touched, fix P0 and P1 in the same change, look at it with `/local-probe`, write the verdict in the pull request; without the skill, review against `0057` and say so. (who decided: owner — "arregla los campos de fecha", "para que cada desarrollo nuevo o cambios la valide, y en caso de que no se cumpla, la arregle"; agent, the shared floor and the severity rule)
- 2026-09-21 — The agent team is files, a protocol and two checks, and its safety does not rest on a definition's frontmatter. Six definitions under `.claude/agents/` — `backend`, `frontend`, `tests` write, each in its own directories; `seo`, `accessibility`, `invariant-reviewer` review and report — led through `/team`, with the rules in `docs/reference/agent-team.md`: the contract before anybody starts, findings sent to whoever owns the file rather than to the lead, reviewers speaking while the screen is being built, and the resources directories do not protect given one owner each (the end-to-end run to `tests`, ports 3000/3001 and the probe to `accessibility`, migration numbers to `backend`, push and merge to the lead). Measured on Claude Code 2.1.278 with teams enabled: an agent started with a name is a teammate, messages travel both ways and wake an agent that had finished, and it gets its definition's prompt and model and **none** of its `tools`, `disallowedTools`, `isolation` or `hooks` — a reviewer denied `Write` had it, an `isolation: worktree` agent ran in the main checkout, a declared hook never fired; and two agents spawned before the session had listed the new types were generic agents wearing the names. So the worktree is made by the agent's first command (`worktree.sh`, which also moves it onto the feature's commit, since Claude Code branches worktrees from `origin/main`, and copies the `.env` files `.worktreeinclude` names); the edit guard is wired for the project in `.claude/settings.json` and knows who is editing from the checkout's branch, `agent/<feature>/<name>`; and the wall is `merge-back.sh`, which refuses uncommitted work left behind, a branch that touched a directory its agent does not own, and any reviewer's branch at all. (who decided: owner — "configúrame todo de forma perfecta para que funcionen en paralelo y se comuniquen entre ellos"; agent, named agents over unnamed ones, identity from the branch, and the merge check as the wall)
- 2026-09-21 — The owner's standing rules are enforced, not remembered. Eleven hookify rules under `.claude/` stop a session from printing a real `.env`, from using the stash, from skipping a check with no-verify, a force push or an `--admin` merge, from dumping process command lines, from putting a credential on a command line, from driving production with the `vercel` CLI and from a recursive delete of a root or a home; three more only warn, where a pattern cannot tell a right use from a wrong one: the production database named in a command, a delete or an overwrite, and a file gaining an absolute home path or something shaped like a credential. They are committed despite their `.local.md` name, because an agent's worktree has only what git has. Two things hookify does not do were measured and built around it: it tells the person why a call was stopped and the agent only that it was, and it looks for its rules in the current directory, so a change of directory switches them off. `rules-to-the-model.py` runs the same files through hookify's own engine from the project root and hands the reason to the agent, as a denial's reason or as added context for a warning; checked in fresh sessions, the agent quoted the alternative it had been given, after a change of directory too. What hookify cannot see at all, the Read tool, is denied natively on the real `.env` files in `.claude/settings.json`. The first version of the patterns stopped this session twice for naming a command inside a prompt and inside a test, so every pattern now requires the command to be run, at the start or after a separator, and a bench of eighty-one cases holds each rule to what it must stop and to what it must let through; two rules were wrong until it ran: three dashes in a pattern cut hookify's frontmatter and left a rule matching every file, and the engine's case-insensitivity made `-d` look like `-D`. A session started in a subdirectory reads no project settings at all, which no file here can fix: start it at the root. (who decided: owner — "sigue con el plugin hookify… super controlado, para intentar que ningún error llegue a prod"; agent, which rules block and which warn, the reason reaching the agent, and the bench)
- 2026-09-21 — The orchestrator prices every task, and CI refuses what no test can take back. **Routing**: inside `/team` the lead runs on `fable`, because its mistakes are paid for by every agent after it, and chooses each agent's model at spawn — measured: the Agent tool's `model` outranks the definition's, a `fable` definition spawned with `haiku` ran as `haiku` and kept its prompt; effort cannot be set per agent. A script for anything a machine can do and say in a line, waiting above all (`wait-for-ci.sh`, written after `gh pr checks --watch` returned a false green before Actions had registered its checks); `haiku` for finding and for edits specified to the character; `sonnet` from a complete contract; `opus` when a decision is still open; `fable` for the two reviewers that are floors — `invariant-reviewer` and the new `migration-reviewer` — and for whoever implements authentication, allergy validation or the validation of model output. A task climbs a model only on evidence, and a task smaller than an agent's bootstrap is the lead's. Its first run was one `backend` agent on `sonnet`, from a specification naming files and lines: three coverage gaps closed with tests only, merged after `merge-back.sh` found four files, all its own. `plan-evaluator` is the second new agent: plans measured on the real library, before and after. **CI**: a migration runs against production during the API's build with no staging before it, so the required check now runs `check-migrations.mjs --drift` (a merged migration edited; a destructive statement without a `-- reviewed-destructive:` line; a journal that does not match; a schema changed with no migration), the end-to-end job rehearses the upgrade on `main`'s schema with data when a change brings a migration, tests run **with coverage** — the thresholds existed and nothing ran them, and three were not met: untested functions in core's domain, a guards threshold in the API whose path jest never found, and ui three points under, now a ratchet — and the web app is built with every public page asserted static. `production.yml` asks production what a person would after every deployment and hourly, opens one issue on a wrong answer and closes it on the first right one. Dependabot weekly, grouped. In the repository's settings, secret scanning, push protection, Dependabot alerts and security updates were off on a public repository; they are on. **Not built**: a preview environment, which needs the owner's Neon and Vercel accounts and a decision about copying people's data — `docs/reference/preview-environment.md`. (who decided: owner — "haz todo lo que has recomendado… este agente determinará todas las tareas, cómo repartirlas y qué modelos usará cada agente… el menor número de tokens y garantizar que todo está bien"; agent, the rubric, the floors, the ratchet and what the machine refuses)
- 2026-09-21 — `main` went red an hour after coverage became what CI runs, and nothing said so. The scheduler's and the plan validator's tests search exhaustively and take about two seconds each on a developer's machine; under coverage on CI's shared runner one took 4.7s and another crossed vitest's default 5s — on the push to `main`, after passing on the pull request, and then on two of Dependabot's pull requests. `packages/core` now sets `testTimeout: 30_000`: a limit a correct test reaches by chance is a coin, not a check. Two holes in the process let it go unseen. The deploy watcher checked the host's deployments and never the CI run `main` makes for the merge itself; it now reads that run and calls a red `main` what it is. And the CI waiter read an hour-old green on a pull request that was waiting for a rebase that never came; it now refuses a head that is behind its base (exit 3). The owner noticed before the agent did. Also learnt: a pull request Dependabot opened as a security update before `dependabot.yml` existed cannot be rebased — it says to close it so a new one is made — and several alerts are `security_update_not_possible`: transitive dependencies a parent pins, which need an override written by hand. (who decided: owner — "ya han terminado y han fallado, y no te ha llegado nada?"; agent, the timeout, and the two watchers)
- 2026-09-21 — Dependabot's patch and minor updates merge themselves; a major waits for a person. Switching the alerts on opened eight pull requests within the hour and the owner asked whether each had to be merged by hand. No: unmerged fixes pile up, and a pile of them is worse than none being offered. `dependabot-automerge.yml` sets a patch, a minor or the weekly grouped update to merge the moment the ruleset allows — both required checks green on an up-to-date branch — so what has to be true does not change, only who presses the button; a red run merges nothing, and after the merge the production smoke asks whether production still answers. A major version is left alone and says so on the pull request. *Allow auto-merge* was switched on in the repository's settings for it. The same day, Next.js went to 16.3 by hand (`#61`), closing the two critical advisories: Dependabot's own pull request for it predated `dependabot.yml` and could not be rebased. (who decided: owner — "tengo que mergear todo a mano?"; agent, majors by hand and everything else on green)
- 2026-09-21 — The advisories Dependabot could not fix are fixed with overrides, each scoped to its vulnerable range. With Next.js done, nineteen high alerts were left, every one in a transitive dependency no package of ours names — `brace-expansion` and `js-yaml` under the lint and glob tooling, `multer` under `@nestjs/platform-express`, `browserslist`, `vite`, `esbuild`, `@babel/core`, `smol-toml` — so Dependabot answered `security_update_not_possible`. `pnpm-workspace.yaml` now has an `overrides` block keyed on the **vulnerable range** (`'multer@<2.3.0': '^2.3.0'`), so nothing already safe is forced and no major is crossed; every fix was inside its own major. An entry is a debt: when the parent catches up it matches nothing and is deleted. The one that needed checking was `esbuild@<=0.24.2`, which drizzle-kit's loader pins at 0.18: `generate` still runs on 0.25. No vulnerable version is left in the lockfile; the full gate is green. (who decided: owner — "haz todo lo que me has dicho"; agent, range-scoped keys over blanket ones)
- 2026-09-22 — A provider's rejection is scrubbed of the configured key by value, not only by shape. `redactSecrets` knew four shapes — Anthropic's and Google's prefixes, a bearer header, `api_key=` — and an OpenAI-compatible gateway rejects a bad key with "Incorrect API key provided: <key>", two words and a colon, echoing a gateway key that carries no prefix at all; that message becomes the job row's `errorDetail`, which the job's owner reads back at `GET /meal-plans/jobs/:id`, so the day the key expired the first person to generate would have read the live credential in JSON. Now `AI_SECRETS` — `providerCredentials(env)`, every set `*_API_KEY` — is injected into both clients and `ErrorReporter`, and every value is cut wherever it appears, found by span against the untouched message and merged with whatever the shape patterns also matched there — a value or a pattern run as a second pass over the other's already-redacted text could leave a fragment of the real key standing next to the marker, so nothing here ever sees the other's output; a generic `sk-` pattern joins the shapes for a vendor key a gateway echoes from its own upstream. Not changed: the provider's own words still reach the job row — a self-hosted operator needs "quota exceeded" — and whether an ordinary account should read them at all is a product question left open. (who decided: agent, from the security scan's findings F1 and F8; value over shape, and both)
- 2026-09-22 — The swap allowance was counted inside the transaction and nowhere near safe: under READ COMMITTED two swaps racing for the fifth each counted four before either committed, and both landed — a way past the per-plan quota and, when the library was short, past the model calls it bounds. `PlanRepository.swapMeal` now takes the plan row `FOR UPDATE` in the ownership query, so the second counts after the first has committed and is refused; the swap rows stay the one record, no counter column and no migration. `swaps.e2e-spec.ts` fires three at the last one and expects one 201 and two 429. Recorded as an amendment to [`0015`](./0015-one-redo-a-fortnight-five-swaps-a-plan.md); `packages/core/AGENTS.md` now names the rule beside `0044`'s guarded `UPDATE`. (who decided: agent, on a security finding)
- 2026-09-22 — A generation is started by claiming a slot, not by asking whether one is free. `PlanJobController.start` read `findInFlight`, then the redo allowance, and only then inserted the job — three statements, three snapshots, nothing held between them. Requests fired together each read *nothing in flight* and each started its own pipeline: a fortnight of model calls apiece, of which `meal_plans_one_active_per_user` keeps one, at the end, after all of them are paid for. The check and the insert are now one step in `PlanJobRepository.claim`, under `pg_try_advisory_xact_lock` keyed on the user: there is no row to hold `FOR UPDATE` when the row being defended is the one that does not exist yet, and the `try` form answers "already being generated" instead of parking a request on a pooled connection. The redo is counted **after** the claim, where the chain cannot grow underneath it, and a claim that may not generate is deleted rather than failed — nothing was generated and nobody should read a failure. `failStale` now also sweeps a `queued` row past the cutoff, which is what a claim abandoned mid-request looks like and what the `running` arm could never see. No migration: a partial unique index on `plan_generation_jobs` would say the same thing, but it is immutable once merged and would have run against whatever pairs of in-flight rows production already holds, during the API's build, with no staging in between. The race is proved in `generation.e2e-spec.ts` against a real database, through the controller rather than the route: the route's rate limiter counts a fixed window the suite is already spending, so it answers first and for another reason. (who decided: agent, on a security finding)
- 2026-09-22 — A password-reset or email-verification link is a bearer token good for an account takeover, and both were written to the log in full on every environment. `sendPasswordResetMail` and the new `VerificationMail.ts` (extracted from Better Auth's inline `sendVerificationEmail` hook) now take the validated `env.NODE_ENV` from their caller rather than reading `process.env` themselves, and log the full URL only under `development`/`test`; every other environment logs the user id alone. The gate rests on a real refusal, not a convention: `Env.validation.ts` already rejects a boot where `VERCEL_ENV` is `production` and `NODE_ENV` is not. (who decided: agent, on a security finding)
- 2026-09-22 — The mail transporter accepted a silent downgrade to plaintext: on any port but 465 (implicit TLS) it only *tried* STARTTLS, so an on-path attacker who stripped the STARTTLS advertisement from the SMTP banner got a connection that looked normal and carried a password-reset link, a verification link or a notification in the clear. `requireTLS: !secure` on the transporter now refuses to send at all rather than fall back. (who decided: agent, on a security finding)
- 2026-09-22 — `RateLimitGuard` ran ahead of `SessionGuard` in the global guard order, so every per-account limit it claimed — three generated plans an hour, five pieces of feedback — was really per IP address: one account behind a carrier NAT or a shared office address inherited everyone else's spend, and one abuser behind that address could lock out every legitimate user on it. The order is now `SessionGuard` first, extracted into an exported `GLOBAL_GUARDS` list (`shared/guards/GlobalGuards.ts`) so a spec can pin it without booting the application; the other three global guards were already reading `request.user` and so already depended on this order being right. Authenticating first costs an anonymous flood nothing extra — Better Auth answers a missing or forged cookie without a database read, and `@Public()` routes never consult the session — but a session-less request to a protected route is now refused before the limiter counts it, so an anonymous flood is denied per request rather than throttled; accepted, because the in-process counters were never a real defence against one across Vercel's fanned-out instances, and the case the limiter exists for — an authenticated flood — is unaffected. (who decided: agent, on a security finding)
- 2026-09-22 — `CronSecretGuard` compared the bearer token to `CRON_SECRET` with `===`, which returns as soon as the first differing byte is found — measurably faster for a guess that is wrong in its first character than one that is wrong in its last, in principle enough signal to grind out a 32-byte secret one byte at a time given a precise enough clock. The comparison now runs through `crypto.timingSafeEqual` on two same-length buffers, checked equal-length first since the function throws rather than returns false otherwise (a length mismatch is answered from the length check alone, leaking only how long the secret is — not a secret in itself for a value this project already documents as `openssl rand -base64 32`); non-string header input is compared as an empty buffer instead of throwing. (who decided: agent, on a security finding)
- 2026-09-22 — `EventController.add` read the fortnight's event standing and inserted as two unsynchronised statements, the same shape as the swap and job-claim races found the same day: requests fired together each read a count that still had room, and each inserted — a free-tier cap of three ending with five. There is no row to hold `FOR UPDATE` for this one either, and unlike `PlanJobRepository.claim` a failed try here is not "already done" — two of the same account's own requests (a double submit, a client retry) are both legitimate. `EventRepository.createWithinQuota` takes a `pg_try_advisory_xact_lock` keyed on the user, re-counts the window's events inside it, and asks a `quota` closure the controller supplies whether one more fits; a failed try backs off (jittered, doubling, capped, up to eight attempts) and tries again with no transaction open while it waits, never once pinning a pooled connection. Two things a first pass at this got wrong, both caught before either reached the working tree: a **blocking** `pg_advisory_xact_lock` pins a connection for the whole wait, and ten concurrent requests from one account would have exhausted the pool (`max: 10`) and denied every other account — fixed by the non-blocking `try` form; and a `tier` read once before the retry loop and reused across attempts would judge a later attempt by a tier that no longer held, unlike the row count, which is genuinely re-read fresh each time — fixed by asking `PlanController.tierOf` again inside `quota`, at the same moment as the re-count. `events-quota-race.e2e-spec.ts` fires more requests than the cap allows at one account and proves exactly the cap lands, against a real database. (who decided: agent, on a security finding)
- 2026-09-22 — A scoped scan of `packages/database`, `packages/ui`, `apps/docs` and `apps/cli` — the four trees not yet covered by this repository's earlier scans of `apps/api`, `apps/web` and `packages/core` — found two findings. `packages/database/scripts/backup.ts` opened its Postgres connection with `ssl: 'require'`, which the `postgres` driver maps to `rejectUnauthorized: false`: the channel is encrypted but the server's certificate and hostname are never checked, so a network-positioned attacker could MITM the connection with a self-signed certificate and capture the DB credential plus every backed-up row; loopback targets keep `ssl: 'require'` exactly as before (a first patch attempt dropped the `ssl` key entirely for loopback, silently changing it to no TLS at all — caught by the verifier and fixed in a revision round), every other host — in practice always the production Neon endpoint — now gets `ssl: 'verify-full'`. `apps/docs/src/lib/workspaceDocs.ts`'s `readDoc()` blocked the private, gitignored `docs/local` directory with an exact-case comparison against the route-supplied slug; on a case-insensitive filesystem (macOS, Windows) a case variant like `/workspace/Local/<file>` evaded the blocklist yet still resolved to the real directory, so the slug is now lowercased before the membership check. Neither patched file has coverage in this repo's own type-check/lint/test scripts — `packages/database`'s tsconfig and eslint glob are both `src/**`, and `apps/docs` has no test suite at all — so both changes rest on independent panel review plus a standalone `tsc` pass over `scripts/backup.ts` and a full `next build` of `apps/docs`, not on the project's own tests. (who decided: agent, on a security finding)
- 2026-09-22 — A scoped scan of `.github` and `scripts` — the two trees outside the per-package map, left after `apps/api`, `apps/web`, `packages/core`, `packages/database`, `packages/ui`, `apps/docs` and `apps/cli` were all scanned — found two findings, both in `.github/workflows/security-review.yml`, the LLM-based diff review that runs on every pull request. The reviewer's `--allowedTools` allowlist is a command-string prefix match, not a parsed argument list: `Bash(find:*)` admitted `find -exec`/`-delete`, and — missed by a first patch attempt, caught by the verifier with a real PoC (`git diff --output=/tmp/…`) — `Bash(git diff:*)`/`git show`/`git log`/`git blame` all accept `--output=<file>`/`--contents=<file>`, any of which would let a prompt-injected diff write to an arbitrary path, including `$GITHUB_ENV`, which a later step in the same job reads back in. No fixed flags in front of the `:*` glob close this for a command whose own arguments can write or execute, so the fix removes every `find`/`git` prefix outright: a controlled, non-agent-invoked shell step now captures the diff body itself (a `${RANDOM}`-keyed `GITHUB_OUTPUT` delimiter, unguessable to an attacker authoring the diff beforehand) and hands it to the model as inert prompt text, leaving only `Read`, `Grep`, `Glob` and the write/exec-incapable `ls`. The second finding — the file list and diff content reaching the prompt with no "this is data, not instructions" framing, letting a PR author talk the reviewer out of reporting their own vulnerability — is closed with an added paragraph generalising that framing to the file list too and naming the injection patterns to disregard (claims of prior review, being told to stop or report fewer findings). Both patches touch the same file; written independently against the same base but deliberately into non-overlapping regions, confirmed to apply cleanly in sequence by a real `git apply` in a scratch clone before either was trusted. Neither has coverage in any repo test/lint task (no `actionlint`/`yamllint`, `.github/workflows/*.yml` is in no `turbo.json` task), so both rest on panel review plus an independent YAML-parser check. (who decided: agent, on a security finding)
- 2026-09-23 — The `/team` lead runs on Opus 5.5 at `high` effort instead of `fable`, and prices each agent by model **and effort**. The Agent tool takes a model per spawn but no effort, and a definition's `effort:` frontmatter overrides the session's, so every building or verifying agent (`backend`, `frontend`, `tests`, `seo`, `accessibility`) now exists at three levels: the base at `medium` and `-low`/`-high` variants generated from it by `.claude/skills/team/scripts/effort-variants.mjs`, identical in prompt, hooks and file ownership, checked by CI and the pre-push hook. The lead picks the level by `subagent_type` and always spawns under the plain name, so the mailbox does not change with effort. `invariant-reviewer` and `migration-reviewer` stay `fable` at `high` and `plan-evaluator` at `medium`, with no variants: their floor is the point. Older models are allowed but none is cheaper than its current counterpart (Opus 4.6–4.8 at $5/$25 against Opus 5.5 at $4/$20; Sonnet 4.6 at $3/$15 against Sonnet 5 at $2/$10), and the Agent tool's `model` only takes the four aliases, so one is used only through a definition's full-id `model:`, for a behaviour the current model lacks. Unmeasured: whether a definition's `effort` is dropped silently when the lead spawns it on `haiku`, which has no effort control. (who decided: owner — "el orquestador opus 5.5… tiene que elegir el esfuerzo del resto de agentes… para optimizar tokens"; agent, the variant mechanism)
- 2026-09-23 — Measured the one open point of the entry above: a definition's `effort:` is dropped without an error when the `/team` lead spawns it on `haiku` (`accessibility-low` on `haiku` ran and answered). (who decided: agent, by measurement)
- 2026-09-23 — Project 004 planned on three decisions taken before any code: a professional reaches a client only through a consented, audited link, whose id — never the client's — is what a professional route carries, resolved by one function against the session ([`0059`](./0059-a-professional-reaches-a-client-only-through-a-link.md)); a plan awaiting a professional's review waits in a status of its own, so every surface that reads the active plan hides it unchanged ([`0060`](./0060-a-reviewed-plan-waits-in-its-own-state.md)); and one subscription row per account grants what its price says — personal premium, or a practice including N clients — with a lapse that pauses links and deletes nothing ([`0061`](./0061-a-subscription-grants-what-its-price-says.md), amending `0056`). The whole feature ships behind a `professional` flag that fails off until the owner's legal review. (who decided: owner — the PRD's answers and plans with an included number of clients; agent, the three mechanisms)
- 2026-09-23 — No agent and no project phase runs on `fable` any more; `opus` is the ceiling. Fable spends usage credits that run out — it did in the middle of project 004's first phase, which finished on Opus. The two reviewers that were fixed at `fable` (`invariant-reviewer`, `migration-reviewer`) are now `opus` at `high` and keep their place as floors; the `quality-max` and `tiered` profiles in `AGENTS.md` are opus-based, project 004's phases 2–3 dispatch at `opus @ high`, and the plan template no longer offers `fable`. (who decided: owner — "no vamos a usar fable en los agentes, siempre opus o modelos inferiores")
- 2026-09-24 — Billing is held to an end-to-end suite over real HTTP and a real database (`apps/api/test/billing.e2e-spec.ts`), and the defects it found are fixed. Three rules come out of it (`docs/reference/payments.md` § 4a). First, deleting an account cancels its Stripe subscriptions, and expires its open checkouts, before the account goes, and a Stripe failure refuses the deletion: being charged after deletion is worse than a retry. Second, an event Stripe can never succeed in delivering is acknowledged, not answered 500. Third, a deployment cancels an orphaned subscription only when the subscription carries that deployment's own marker, a hash of the database identity, because local development and production share one Stripe test account and receive each other's events. (who decided: owner — "tiene que ser tolerante a fallos, y super segura", "corrige… todos los que encuentre"; agent, the mechanisms, after `invariant-reviewer`)
- 2026-09-24 — Project 004, two open points from Phase 4 decided before Phases 6 and 7. First, the check-in's kcal nudge never changes targets a professional set while their link is active: the check-in is recorded, and the professional sees the suggested kcal behind the audited read and applies it or not. Second, when a link ends, the targets stay but lose the professional's mark and become the client's own, while a pause keeps the mark. Written into PLAN.md Phase 6 step 4 and Phase 7 step 7. (who decided: owner — accepted both recommendations; agent, the mechanisms)
- 2026-09-24 — Project 004: a professional can start their client's next fortnight once the current one has ended, under the same date rule that offers it to the client. The new plan follows the review switch as a client-started one does and is charged once; a running fortnight stays out of a professional's reach, as Phase 5 guarantees. Written into PLAN.md as Phase 9 step 0 (API first, then the button). (who decided: owner)
