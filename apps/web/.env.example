# The NestJS API's public origin. This is the ONLY variable the web app needs:
# it holds no database connection, no auth secret and no AI key. Every one of
# those lives in apps/api and never crosses the network to the browser.
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1
