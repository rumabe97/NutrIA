/* Styles-only module: the auth pages share AuthForm.module.css rather than a
   component, because each form's fields and submit behaviour differ enough that
   a shared component would be a prop soup. */
export {};
