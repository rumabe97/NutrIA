import type { ProfileConsentView } from 'core/controllers/Profile';

/** The profile consent as it now stands: the version to give, whether it is given, and when. */
export type ProfileConsentDto = ProfileConsentView;
