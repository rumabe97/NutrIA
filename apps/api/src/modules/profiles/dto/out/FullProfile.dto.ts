import type { FullProfileView } from 'core/controllers/Profile';

/** Profile, goal, preferences, restrictions and the resolved daily targets, in one read. */
export type FullProfileDto = FullProfileView;
