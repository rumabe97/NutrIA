import type { AllowancesView } from 'core/controllers/Plan';

/** What the fortnight still allows: a redo, how many swaps (`0015`), how many events and whether one may rebuild it (`0044`). */
export type AllowancesDto = AllowancesView;
