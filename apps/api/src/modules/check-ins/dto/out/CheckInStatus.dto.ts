import type { CheckInStatusView } from 'core/controllers/CheckIn';

/** Whether the check-in is due, whether it is done, and how the fortnight went. */
export type CheckInStatusDto = CheckInStatusView;
