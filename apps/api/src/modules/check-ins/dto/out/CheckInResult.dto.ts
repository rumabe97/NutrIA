import type { CheckInResultView } from 'core/controllers/CheckIn';

/** What closing the fortnight changed: the log, the targets, the next plan's prompt. */
export type CheckInResultDto = CheckInResultView;
